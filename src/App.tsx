import React, { useState, useEffect, useRef } from "react";
import { motion, AnimatePresence } from "motion/react";
import { 
  Wind, Sparkles, Brain, Timer, Play, Pause, Flame, User, Heart, Compass, 
  TrendingUp, Dumbbell, Music, Volume2, VolumeX, Award, ShieldAlert, Send, 
  RotateCcw, Check, ArrowRight, ChevronRight, Info, X, ChevronDown, CheckCircle2,
  Calendar, Smile, BookOpen, Layers, CheckCircle
} from "lucide-react";
import Logo from "./components/Logo";
import PoseVisualizer from "./components/PoseVisualizer";
import { PranaAudio } from "./components/AudioEngine";
import { UserProfile, YogaPose, YogaPlan, ChatMessage, PRESET_POSES } from "./types";

// Daily motivational quotes from ancient sages and modernized bio-energetic wisdom
const DAILY_QUOTES = [
  { text: "Heal the body, quiet the mind, realize the eternal light within.", author: "Patanjali Yoga Sutras" },
  { text: "When the breath wanders, the mind also is unsteady. But when the breath is calmed, the mind too will be still.", author: "Hatha Yoga Pradipika" },
  { text: "Tapas is the heat generated by yogic posture that burns away all mental impurity.", author: "Sanskrit Wisdom" },
  { text: "The very heart of yoga practice is 'Abhyasa'—steady effort in the direction of peaceful stillness.", author: "Yoga Guru" },
  { text: "Your body is a cosmic receiver. Adjust its posture, tune its frequency, download infinite peace.", author: "Acharya" }
];

const HEALTHY_JOKES = [
  {
    setup: "Why did the tomato go to yoga class? 🍅",
    punchline: "To get a little more flexible, but it ended up as cute organic ketchup!",
    funnyRating: "🍅 Tomato Squeeze Level: 10/10"
  },
  {
    setup: "What does the yoga teacher say when they are frustrated? 🧘‍♀️",
    punchline: "I am passing on my bad karma to you, but take a deep deep breath first!",
    funnyRating: "✨ Karma Level: Harmonized"
  },
  {
    setup: "Why did the adorable sloth refuse to do downward-facing dog? 🦥",
    punchline: "Because it preferred the 'sleeping sloth pose' (Balasana) with extra fluffy pastel cloud pillows!",
    funnyRating: "☁️ Cozy Sloth Score: Snuggly Great"
  },
  {
    setup: "Why is the avocado outstanding at daily meditation? 🥑",
    punchline: "Because it's always centered on its inner peace... and its friendly giant seed!",
    funnyRating: "🥑 Seed Centering: Perfectly Balanced"
  },
  {
    setup: "What did the yogi tell the officer who pulled them over for speeding? 🚔",
    punchline: "Officer, I was just trying to flow through this fast-paced material dimension as mindfully as possible!",
    funnyRating: "🚨 Ticket Escape Rate: 99%"
  },
  {
    setup: "Why do normal people make the most hilarious yogis? 🍕",
    punchline: "Because when they are told to 'empty their minds', they immediately start planning their post-workout cheese pizza slice!",
    funnyRating: "🍕 Pizza Desire index: High"
  },
  {
    setup: "What did the wise yoga instructor say to the smartphone? 📱",
    punchline: "Please remember to align your spine, dear! You've been stuck in the 'Heavy Scroll-asana' pose for five hours!",
    funnyRating: "⚡ Spine Deflection Alert: 100%"
  }
];

const HEALTHY_RECIPES = [
  {
    name: "Divine Avocado Green Smoothie 🥑",
    slogan: "Cellular hydrating green magic potion",
    prepTime: "5 mins",
    difficulty: "Easy Peasy",
    nutrition: { kcal: "280 kcal", protein: "6g", fats: "12g", fiber: "8g" },
    ingredients: [
      "1/2 ripe Haas Avocado (for chunky texture)",
      "1 cup Organic Spinach leaves",
      "1 frozen fresh Banana",
      "1 cup Unsweetened Almond Milk",
      "1 tablespoon Chia seeds (for super omega-3!)",
      "1 teaspoon raw Maple Syrup or Honey"
    ],
    steps: [
      "Add all the superfood ingredients together in your blender container.",
      "Blend on high power until thick, creamy, and beautifully pastel green.",
      "Pour into your favorite cute glass and garnish with chia seeds or shredded coconut.",
      "Take 3 deep, happy breaths first, then sip mindfully!"
    ],
    chefTip: "🥑 Swap almond milk with coconut cream to make it taste like a tropical wellness smoothie!"
  },
  {
    name: "Sun Salutation Golden Milk ☕",
    slogan: "Warm Ayurvedic joint & recovery nectar",
    prepTime: "8 mins",
    difficulty: "Cute & Warm",
    nutrition: { kcal: "140 kcal", protein: "2g", fats: "8g", fiber: "1.5g" },
    ingredients: [
      "1.5 cups Unsweetened Coconut Milk",
      "1 teaspoon organic Turmeric powder",
      "1/2 teaspoon shredded Cinnamon",
      "A small pinch of Black Pepper (Crucial! It boosts turmeric absorption by 2000%!)",
      "1 teaspoon pure Maple Syrup",
      "A sliver of fresh Ginger"
    ],
    steps: [
      "Pour coconut milk into a friendly small saucepan over medium heat.",
      "Whisk in the golden turmeric, cinnamon, black pepper, and ginger slices.",
      "Stir gently for 5 minutes without bringing it to a heavy boil.",
      "Strain into a cozy warm mug, stir in mapping syrup, and sip comfortably near your yoga mat."
    ],
    chefTip: "✨ Drinking this right before bed relaxes the nervous system and aids deep recovery sleep."
  },
  {
    name: "Sacred Quinoa Power Bowl 🥗",
    slogan: "High-energy cellular building blocks",
    prepTime: "15 mins",
    difficulty: "Super Satisfying",
    nutrition: { kcal: "410 kcal", protein: "14g", fats: "9g", fiber: "11g" },
    ingredients: [
      "1 cup cooked Tri-Color Quinoa",
      "1/2 roasted Sweet Potato (cubed with love)",
      "1/2 cup organic Steamed Chickpeas",
      "Fresh crunchy Cucumber slices",
      "1 tablespoon Creamy Tahini paste",
      "A squeeze of fresh Lemon juice"
    ],
    steps: [
      "Spoon cooked fluffy quinoa as a soft base layer in your favorite wide bowl.",
      "Arrange sweets, chickpeas, and fresh cucumbers in cute little columns.",
      "Drizzle creamy, nutty tahini paste evenly across the top.",
      "Squeeze a fresh yellow lemon on top and toss happily before dining."
    ],
    chefTip: "🥕 Throw in shredded purple cabbage or bright orange carrots to make it look like a cute rainbow!"
  },
  {
    name: "Blissful Berry Chia Pudding 🍓",
    slogan: "Wholesome fiber guilt-free desert snack",
    prepTime: "5 mins prep (+2h chill)",
    difficulty: "Easy Cooling",
    nutrition: { kcal: "210 kcal", protein: "5g", fats: "7g", fiber: "9g" },
    ingredients: [
      "3 tablespoons organic Chia Seeds",
      "1 cup Soy or Coconut Milk",
      "1/2 cup mixed fresh Strawberries & Blueberries",
      "1/2 teaspoon Vanilla extract",
      "Fresh mint leaf (for a cute visual garnish!)"
    ],
    steps: [
      "Stir chia seeds, vanilla extract, and cold milk together in a pretty glass jar.",
      "Wait 5 minutes, stir well again to prevent clamping, then lid the jar.",
      "Refrigerate for at least 2 hours (or overnight) to let the chia seeds puff into a thick pudding.",
      "Mash half of the sweet berries, spoon them on top as a cute jam layer, and dig in!"
    ],
    chefTip: "🥄 Layer with gluten-free oat granola for a delightful crispy crunch!"
  }
];

export default function App() {
  // Navigation & Lifecycle
  const [showIntro, setShowIntro] = useState(true);
  const [activeTab, setActiveTab] = useState<"home" | "assessment" | "practice" | "tracker" | "poses" | "cute" | "jokes">("home");
  const [quoteIndex, setQuoteIndex] = useState(0);

  // Dedicated Rich Jokes Page States
  const [userJokes, setUserJokes] = useState<{setup: string, punchline: string, funnyRating: string, category: string, id: string}[]>(() => {
    const saved = localStorage.getItem("yogasan_user_jokes");
    return saved ? JSON.parse(saved) : [];
  });
  const [jokeVotes, setJokeVotes] = useState<Record<string, number>>(() => {
    const saved = localStorage.getItem("yogasan_joke_votes_map");
    return saved ? JSON.parse(saved) : {};
  });
  const [jokeCategory, setJokeCategory] = useState<string>("all");
  const [copiedJokeId, setCopiedJokeId] = useState<string | null>(null);
  const [isSpeakingJokeId, setIsSpeakingJokeId] = useState<string | null>(null);

  // Form states for adding custom jokes
  const [newSetup, setNewSetup] = useState("");
  const [newPunchline, setNewPunchline] = useState("");
  const [newCategory, setNewCategory] = useState("Asana");
  const [newEmoji, setNewEmoji] = useState("🥑");

  // Automated Laughter (Hasya) Yoga sequence trainer
  const [hasyaStep, setHasyaStep] = useState(0);
  const [hasyaTimer, setHasyaTimer] = useState(0);

  // Audio Engine Client States
  const [soundActive, setSoundActive] = useState(false);
  const [soundFrequency, setSoundFrequency] = useState<"432" | "528" | "639">("432");
  const [soundVolume, setSoundVolume] = useState(0.15);

  // User Profile State (persisted in LocalStorage)
  const [profile, setProfile] = useState<UserProfile>(() => {
    const saved = localStorage.getItem("yogasan_profile");
    if (saved) {
      try { return JSON.parse(saved); } catch (e) {}
    }
    return {
      name: "Yogi Guest",
      age: 28,
      gender: "Non-binary",
      height: 172,
      weight: 68,
      flexibility: "Moderate",
      experience: "Beginner",
      goal: "Stress Relief"
    };
  });

  // User Assessment Input State (Form Temp State)
  const [formName, setFormName] = useState(profile.name);
  const [formAge, setFormAge] = useState(profile.age);
  const [formGender, setFormGender] = useState(profile.gender);
  const [formHeight, setFormHeight] = useState(profile.height);
  const [formWeight, setFormWeight] = useState(profile.weight);
  const [formFlexibility, setFormFlexibility] = useState<UserProfile["flexibility"]>(profile.flexibility);
  const [formExperience, setFormExperience] = useState<UserProfile["experience"]>(profile.experience);
  const [formGoal, setFormGoal] = useState<UserProfile["goal"]>(profile.goal);

  // Assessment Loading & Active Plan
  const [isAssessing, setIsAssessing] = useState(false);
  const [curatedPlan, setCuratedPlan] = useState<YogaPlan | null>(null);
  const [planSource, setPlanSource] = useState("Sanctuary Wisdom Library");

  // Chatbot Teacher States
  const [chatMessages, setChatMessages] = useState<ChatMessage[]>([
    {
      id: "welcome-msg",
      sender: "teacher",
      text: "Namaste 🙏. I am Acharya, your personal AI Yoga Guide. I draw from classical scriptures and physical rehabilitation science to curate a path aligned with your lifespan goals. Ask me any posture adjustments, breathing depths, or general injuries to guard!",
      timestamp: "03:55 AM"
    }
  ]);
  const [chatInput, setChatInput] = useState("");
  const [isChatTyping, setIsChatTyping] = useState(false);
  const chatEndRef = useRef<HTMLDivElement>(null);

  // Immersive Active Practice Session States
  const [isPracticeMode, setIsPracticeMode] = useState(false);
  const [activePoseIdx, setActivePoseIdx] = useState(0);
  const [timeRemaining, setTimeRemaining] = useState(0);
  const [timerRunning, setTimerRunning] = useState(false);
  const [practiceCompleted, setPracticeCompleted] = useState(false);
  
  // Audio Speech Assist
  const [voiceExplanation, setVoiceExplanation] = useState<string | null>(null);

  // Stats Analytics tracker
  const [weeklyHistory, setWeeklyHistory] = useState(() => {
    const saved = localStorage.getItem("yogasan_history");
    return saved ? JSON.parse(saved) : [
      { date: "May 22", duration: 15, calories: 110, completedCount: 1 },
      { date: "May 24", duration: 10, calories: 75, completedCount: 1 },
      { date: "May 25", duration: 24, calories: 185, completedCount: 2 },
      { date: "May 27", duration: 18, calories: 130, completedCount: 1 },
    ];
  });
  const [streakCount, setStreakCount] = useState(() => {
    return parseInt(localStorage.getItem("yogasan_streak") || "4", 10);
  });
  const [completedDays, setCompletedDays] = useState<string[]>(() => {
    const saved = localStorage.getItem("yogasan_streak_days");
    return saved ? JSON.parse(saved) : ["Mon", "Tue", "Wed", "Thu"];
  });
  const [allTimeHighStreak, setAllTimeHighStreak] = useState(() => {
    return parseInt(localStorage.getItem("yogasan_high_streak") || "7", 10);
  });
  const [fannedFlameCount, setFannedFlameCount] = useState(() => {
    return parseInt(localStorage.getItem("yogasan_fanned_flame") || "3", 10);
  });

  // Selected Pose Detail Modal
  const [selectedPose, setSelectedPose] = useState<YogaPose | null>(null);

  // Cozy Corner content selectors
  const [selectedRecipeIdx, setSelectedRecipeIdx] = useState<number>(0);
  const [currentJokeIdx, setCurrentJokeIdx] = useState<number>(0);

  // Cute Avocado companion pet states
  const [avocadoMood, setAvocadoMood] = useState<"ZEN" | "CRAZY" | "SLEEPY" | "HAPPY">("ZEN");
  const [avocadoDialogue, setAvocadoDialogue] = useState<string>("Namaste! I am your cuddly companion. Tap me for cute ticks, giggles, or change my mood!");

  // Auto-scroll chat to bottom
  useEffect(() => {
    chatEndRef.current?.scrollIntoView({ behavior: "smooth" });
  }, [chatMessages, isChatTyping]);

  // Rotates daily quote index every 10 seconds
  useEffect(() => {
    const quoteInterval = setInterval(() => {
      setQuoteIndex((prev) => (prev + 1) % DAILY_QUOTES.length);
    }, 10000);
    return () => clearInterval(quoteInterval);
  }, []);

  // Timer counter loop for practice room
  useEffect(() => {
    let timerId: any = null;
    if (timerRunning && timeRemaining > 0) {
      timerId = setInterval(() => {
        setTimeRemaining((prev) => {
          if (prev <= 1) {
            handlePoseTimeUp();
            return 0;
          }
          return prev - 1;
        });
      }, 1000);
    }
    return () => {
      if (timerId) clearInterval(timerId);
    };
  }, [timerRunning, timeRemaining]);

  // Sync profile update to LocalStorage and automatically calculate custom fallback on start
  useEffect(() => {
    localStorage.setItem("yogasan_profile", JSON.stringify(profile));
    // Default initial load calculation
    recalculatePlanLocal(profile);
  }, [profile]);

  // Initialize offline synth volume bind
  useEffect(() => {
    PranaAudio.changeVolume(soundVolume);
  }, [soundVolume]);

  // Automated Laughter (Hasya) Yoga sequence trainer countdown timer
  useEffect(() => {
    let interval: any = null;
    if (hasyaStep > 0 && hasyaTimer > 0) {
      interval = setInterval(() => {
        setHasyaTimer((prev) => {
          if (prev <= 1) {
            if (hasyaStep < 5) {
              setHasyaStep((prevS) => prevS + 1);
              playChimeSound();
              return 6; // duration of succeeding steps
            } else {
              setHasyaStep(0);
              playSuccessFanfare();
              return 0;
            }
          }
          return prev - 1;
        });
      }, 1000);
    }
    return () => {
      if (interval) clearInterval(interval);
    };
  }, [hasyaStep, hasyaTimer]);

  const recalculatePlanLocal = (userP: UserProfile) => {
    // Synchronous locally generated baseline plans as safety presets
    const age = userP.age;
    const goal = userP.goal;
    const flexibility = userP.flexibility;
    const experience = userP.experience;

    let baselinePoses: YogaPose[] = [];
    let tagline = `Personalized path tailored for your ${flexibility} flexibility and ${goal} aspiration.`;
    let title = "Universal Serene Prana Flow";
    let difficulty: YogaPlan["difficulty"] = "Moderate";
    let safetyAdvice = "Stay incredibly gentle with physical extension. Stop immediately if any joints feel acute compression.";
    let mindsetQuote = "Health is dynamic equilibrium. Yoga is the tuning of this fine balance.";

    const allP = Object.values(PRESET_POSES);

    if (age < 12) {
      title = "Playful Lotus Discovery Flow";
      difficulty = "Gentle";
      baselinePoses = [
        PRESET_POSES.tadasana,
        PRESET_POSES.vrikshasana,
        PRESET_POSES.marjaryasana,
        PRESET_POSES.balasana
      ];
      safetyAdvice = "Ensure parents inspect for soft posture landing. Garnish sessions with laughter and lightweight balance.";
      mindsetQuote = "Stand brave and high like a green pine tree, relax your mind like a mountain stream.";
    } else if (age >= 60) {
      title = "Restorative Golden Age Alignment";
      difficulty = "Gentle";
      baselinePoses = [
        PRESET_POSES.sukhasana,
        PRESET_POSES.marjaryasana,
        PRESET_POSES.balasana,
        PRESET_POSES.bhujangasana
      ];
      safetyAdvice = "For maximum safety, perform transitions on comfortable foam mats and avoid extreme backbending angles.";
      mindsetQuote = "Age is a measurement of physical time, spirit remains forever free and flexible.";
    } else {
      if (goal === "Weight Loss" || goal === "Muscle Strength") {
        title = "Metabolic Tapas Energy Igniter";
        difficulty = experience === "Advanced" ? "Advanced" : "Challenging";
        baselinePoses = [
          PRESET_POSES.tadasana,
          PRESET_POSES.adhomukha,
          PRESET_POSES.virabhadrasana,
          PRESET_POSES.bhujangasana
        ];
        safetyAdvice = "Engage deep core stabilizers firmly to preserve absolute alignment. Sweat is merely toxins yielding under heat.";
        mindsetQuote = "Discipline is the forge of self-transformation. Enter the fire of active movement.";
      } else if (goal === "Meditation" || goal === "Stress Relief") {
        title = "Sahasrara Bliss Mind Oasis";
        difficulty = "Gentle";
        baselinePoses = [
          PRESET_POSES.sukhasana,
          PRESET_POSES.balasana,
          PRESET_POSES.marjaryasana
        ];
        safetyAdvice = "Incorporate deep, soothing diaphragmatic nose breathing cycles. Keep eyes gently soft/closed.";
        mindsetQuote = "You are not your thoughts; you are the silent space where they arise and dissipate.";
      } else {
        // Back Pain Relief or Flexibility
        title = "Spinal Decompression & Lotus Opening";
        difficulty = flexibility === "Beginner" ? "Gentle" : "Moderate";
        baselinePoses = [
          PRESET_POSES.marjaryasana,
          PRESET_POSES.adhomukha,
          PRESET_POSES.bhujangasana,
          PRESET_POSES.balasana
        ];
        safetyAdvice = "Focus strictly on lengthening the intervertebral spaces rather than hyper-extending the lower back muscles.";
        mindsetQuote = "A flexible body houses a flexible mind. Let go of all toxic physical rigidities.";
      }
    }

    const totalC = baselinePoses.reduce((s, p) => s + p.calories, 0);
    const totalD = `${Math.round(baselinePoses.reduce((s, p) => s + p.durationSeconds, 0) / 60)} Minutes`;

    setCuratedPlan({
      title,
      tagline,
      difficulty,
      totalDuration: totalD,
      totalCalories: totalC,
      safetyAdvice,
      mindsetQuote,
      poses: baselinePoses
    });
  };

  // Audio Toggle
  const handleToggleSound = (type?: "432" | "528" | "639") => {
    const selectedType = type || soundFrequency;
    const stateActive = PranaAudio.toggle(undefined, selectedType);
    setSoundActive(stateActive);
    if (type) setSoundFrequency(type);
  };

  // Sound Engine Setup & Initial Sound Active trigger helper
  const handleFrequencyChange = (freq: "432" | "528" | "639") => {
    setSoundFrequency(freq);
    if (soundActive) {
      // Toggle off and on to swap frequencies gracefully
      PranaAudio.toggle(false);
      setTimeout(() => {
        PranaAudio.toggle(true, freq);
      }, 50);
    } else {
      PranaAudio.toggle(true, freq);
      setSoundActive(true);
    }
  };

  // Form Submission
  const handleFormSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setIsAssessing(true);
    
    // Construct Profile
    const updatedProfile: UserProfile = {
      name: formName || "Yogi",
      age: Number(formAge),
      gender: formGender,
      height: Number(formHeight),
      weight: Number(formWeight),
      flexibility: formFlexibility,
      experience: formExperience,
      goal: formGoal
    };

    setProfile(updatedProfile);

    try {
      const response = await fetch("/api/assess", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ profile: updatedProfile })
      });
      const data = await response.json();
      if (data && data.plan) {
        setCuratedPlan(data.plan);
        setPlanSource(data.source || "Live Acharya AI Core");
      } else {
        recalculatePlanLocal(updatedProfile);
        setPlanSource("Dynamic Local Presets");
      }
    } catch (err) {
      console.warn("Express API unreachable. Resorting to smart offline local yoga compiler.", err);
      recalculatePlanLocal(updatedProfile);
      setPlanSource("Guaranteed Sanctuary Wise Offline Plan");
    } finally {
      setIsAssessing(false);
      setActiveTab("home");
      // Scroll smoothly to recommended section
      setTimeout(() => {
        const sect = document.getElementById("recommended-section");
        sect?.scrollIntoView({ behavior: "smooth", block: "start" });
      }, 300);
    }
  };

  // Chat request dispatch
  const handleSendChatMessage = async () => {
    if (!chatInput.trim()) return;
    const userMsgText = chatInput;
    const userMsg: ChatMessage = {
      id: `user-${Date.now()}`,
      sender: "user",
      text: userMsgText,
      timestamp: new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })
    };

    setChatMessages(prev => [...prev, userMsg]);
    setChatInput("");
    setIsChatTyping(true);

    try {
      const response = await fetch("/api/chat", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ 
          message: userMsgText,
          history: chatMessages.slice(-5) // Send last 5 chats as context
        })
      });
      const data = await response.json();
      const teacherMsg: ChatMessage = {
        id: `acharya-${Date.now()}`,
        sender: "teacher",
        text: data.reply || "Namaste. I am in deep meditative flow. Continue breathing calmly.",
        timestamp: new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })
      };
      setChatMessages(prev => [...prev, teacherMsg]);
    } catch (err) {
      // offline smart fallback replies
      setTimeout(() => {
        let reply = "Namaste 🙏. I am connecting deep with the local spirit database. ";
        const lower = userMsgText.toLowerCase();
        if (lower.includes("pain") || lower.includes("stiff") || lower.includes("back") || lower.includes("injury")) {
          reply += "For spinal pain, practice Tabletop dynamic movements like Hatha Cat-Cow extremely gently. Never force your thoracic cage.";
        } else if (lower.includes("pregnant") || lower.includes("pregnancy") || lower.includes("child")) {
          reply += "During beautiful maternity transitions, limit heavy abdominal compressions. Gentle child's pose with wide knees is comforting.";
        } else if (lower.includes("breathe") || lower.includes("pranayama") || lower.includes("anxious")) {
          reply += "Focus on Alternate Nostril Breathing (Nadi Shodhana) to immediately harmonize left/right brain hemispheres and slow heart rate.";
        } else {
          reply += "Rest your heels, roll your neck shoulder joints gently, and feel of cosmic life force (Prana) filling your respiratory system. How does your body feel today?";
        }
        setChatMessages(prev => [...prev, {
          id: `acharya-local-${Date.now()}`,
          sender: "teacher",
          text: reply,
          timestamp: new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })
        }]);
      }, 950);
    } finally {
      setIsChatTyping(false);
    }
  };

  // Practice session state changers
  const startPracticeRoom = () => {
    if (!curatedPlan || curatedPlan.poses.length === 0) return;
    setIsPracticeMode(true);
    setActivePoseIdx(0);
    setTimeRemaining(curatedPlan.poses[0].durationSeconds);
    setTimerRunning(true);
    setPracticeCompleted(false);
    setVoiceExplanation(null);
  };

  const handlePoseTimeUp = () => {
    // play synthesized chime using audio engine
    playChimeSound();

    if (curatedPlan && activePoseIdx < curatedPlan.poses.length - 1) {
      // Advance to next pose
      const nextIdx = activePoseIdx + 1;
      setActivePoseIdx(nextIdx);
      setTimeRemaining(curatedPlan.poses[nextIdx].durationSeconds);
      setVoiceExplanation(null);
    } else {
      // Practice completed!
      setTimerRunning(false);
      setPracticeCompleted(true);
      recordPracticeCompletion();
    }
  };

  const skipToNextPose = () => {
    handlePoseTimeUp();
  };

  const restartPracticeSession = () => {
    startPracticeRoom();
  };

  const recordPracticeCompletion = () => {
    if (!curatedPlan) return;
    // Add completed session to stats
    const todayStr = new Date().toLocaleDateString([], { month: 'short', day: 'numeric' });
    const durationMins = Math.round(curatedPlan.poses.reduce((sum, p) => sum + p.durationSeconds, 0) / 60);
    const burnedCal = curatedPlan.totalCalories;

    const newHist = [...weeklyHistory];
    const indexToday = newHist.findIndex(h => h.date === todayStr);
    if (indexToday >= 0) {
      newHist[indexToday].duration += durationMins;
      newHist[indexToday].calories += burnedCal;
      newHist[indexToday].completedCount += 1;
    } else {
      newHist.push({ date: todayStr, duration: durationMins, calories: burnedCal, completedCount: 1 });
    }

    if (newHist.length > 6) {
      newHist.shift(); // Keep last 6 sessions
    }

    setWeeklyHistory(newHist);
    localStorage.setItem("yogasan_history", JSON.stringify(newHist));

    // Increment streak
    const newStreak = streakCount + 1;
    setStreakCount(newStreak);
    localStorage.setItem("yogasan_streak", newStreak.toString());
  };

  // Synthesize beautiful Web Audio API chime tone
  const playChimeSound = () => {
    try {
      const AudioCtxClass = window.AudioContext || (window as any).webkitAudioContext;
      if (!AudioCtxClass) return;
      const actx = new AudioCtxClass();
      
      const osc = actx.createOscillator();
      const gain = actx.createGain();
      
      osc.type = "sine";
      osc.frequency.setValueAtTime(880, actx.currentTime); // high pure harmonic chime pitch A5
      osc.frequency.exponentialRampToValueAtTime(110, actx.currentTime + 1.5);
      
      gain.gain.setValueAtTime(0.3, actx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.001, actx.currentTime + 1.5);
      
      osc.connect(gain);
      gain.connect(actx.destination);
      
      osc.start();
      osc.stop(actx.currentTime + 1.6);
    } catch (e) {
      console.warn("Browser blocks audio context initialization or chime play.", e);
    }
  };

  // Synthesize a cute, high pop speech bubble click noise
  const playBubblePop = () => {
    try {
      const AudioCtxClass = window.AudioContext || (window as any).webkitAudioContext;
      if (!AudioCtxClass) return;
      const actx = new AudioCtxClass();
      const osc = actx.createOscillator();
      const gain = actx.createGain();
      
      osc.type = "sine";
      osc.frequency.setValueAtTime(320, actx.currentTime);
      osc.frequency.exponentialRampToValueAtTime(1200, actx.currentTime + 0.12);
      
      gain.gain.setValueAtTime(0.2, actx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.001, actx.currentTime + 0.12);
      
      osc.connect(gain);
      gain.connect(actx.destination);
      osc.start();
      osc.stop(actx.currentTime + 0.13);
    } catch (e) {}
  };

  // Synthesize a funny cartoon slide pitch
  const playSillySlide = () => {
    try {
      const AudioCtxClass = window.AudioContext || (window as any).webkitAudioContext;
      if (!AudioCtxClass) return;
      const actx = new AudioCtxClass();
      const osc = actx.createOscillator();
      const gain = actx.createGain();
      
      osc.type = "triangle";
      osc.frequency.setValueAtTime(440, actx.currentTime);
      osc.frequency.linearRampToValueAtTime(220, actx.currentTime + 0.2);
      osc.frequency.linearRampToValueAtTime(660, actx.currentTime + 0.4);
      
      gain.gain.setValueAtTime(0.25, actx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.001, actx.currentTime + 0.45);
      
      osc.connect(gain);
      gain.connect(actx.destination);
      osc.start();
      osc.stop(actx.currentTime + 0.48);
    } catch (e) {}
  };

  // Synthesize cozy low frequency cartoon snore puffs
  const playSnoreSound = () => {
    try {
      const AudioCtxClass = window.AudioContext || (window as any).webkitAudioContext;
      if (!AudioCtxClass) return;
      const actx = new AudioCtxClass();
      const osc = actx.createOscillator();
      const gain = actx.createGain();
      
      osc.type = "sawtooth";
      osc.frequency.setValueAtTime(75, actx.currentTime);
      osc.frequency.linearRampToValueAtTime(105, actx.currentTime + 0.45);
      osc.frequency.linearRampToValueAtTime(75, actx.currentTime + 0.9);
      
      gain.gain.setValueAtTime(0.12, actx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.001, actx.currentTime + 0.95);
      
      osc.connect(gain);
      gain.connect(actx.destination);
      osc.start();
      osc.stop(actx.currentTime + 1.0);
    } catch (e) {}
  };

  // Synthesize a quick cartoon victory fanfare!
  const playSuccessFanfare = () => {
    try {
      const AudioCtxClass = window.AudioContext || (window as any).webkitAudioContext;
      if (!AudioCtxClass) return;
      const actx = new AudioCtxClass();
      
      // We trigger 3 quick successive notes for a happy cheerful chime
      const notes = [523.25, 659.25, 783.99, 1046.50]; // C5, E5, G5, C6
      const times = [0, 0.08, 0.16, 0.24];
      
      notes.forEach((freq, idx) => {
        const osc = actx.createOscillator();
        const gain = actx.createGain();
        osc.type = "triangle";
        osc.frequency.setValueAtTime(freq, actx.currentTime + times[idx]);
        
        gain.gain.setValueAtTime(0.15, actx.currentTime + times[idx]);
        gain.gain.exponentialRampToValueAtTime(0.001, actx.currentTime + times[idx] + 0.3);
        
        osc.connect(gain);
        gain.connect(actx.destination);
        
        osc.start(actx.currentTime + times[idx]);
        osc.stop(actx.currentTime + times[idx] + 0.35);
      });
    } catch (e) {}
  };

  // Dynamic custom explanation bubble trigger
  const triggerVoiceExplanation = (pose: YogaPose) => {
    if (voiceExplanation) {
      setVoiceExplanation(null);
    } else {
      // Simulate beautiful direct AI vocal feedback instruction readable text
      const speech = `“To master ${pose.name} (${pose.sanskritName}), align your center, pull your crown upward, and breathe softly. Breathe in pure, vitalized oxygen. Exhale all metabolic waste and fatigue. Focus your inner chakra point dynamically.”`;
      setVoiceExplanation(speech);
      playChimeSound(); // chime to register prompt trigger
    }
  };

  return (
    <div className="min-h-screen bg-white text-slate-800 flex flex-col relative overflow-x-hidden selection:bg-purple-200 selection:text-purple-900" id="main-yogasan-master">
      
      {/* Dynamic Background warm, premium light lavender aesthetic with soft glowing highlights */}
      <div className="absolute inset-0 pointer-events-none overflow-hidden z-0" id="cosmic-background">
        <div className="absolute top-[5%] left-[10%] w-[400px] h-[400px] rounded-full bg-purple-200/40 blur-[110px] animate-pulse" />
        <div className="absolute bottom-[15%] right-[5%] w-[450px] h-[450px] rounded-full bg-pink-200/40 blur-[120px] animate-pulse" style={{ animationDuration: '10s' }} />
        <div className="absolute top-[40%] left-[45%] -translate-x-1/2 -translate-y-1/2 w-[650px] h-[650px] rounded-full bg-indigo-100/35 blur-[140px] pointer-events-none" />
        <div className="absolute inset-0 bg-[linear-gradient(to_right,#ebdff5_1px,transparent_1px),linear-gradient(to_bottom,#ebdff5_1px,transparent_1px)] bg-[size:4.5rem_4.5rem] [mask-image:radial-gradient(ellipse_60%_50%_at_50%_40%,#000_80%,transparent_100%)] opacity-40" />
      </div>

      {/* 1. ANIMATED INTRO / SPLASH SCREEN */}
      <AnimatePresence>
        {showIntro && (
          <motion.div 
            initial={{ opacity: 1 }}
            exit={{ opacity: 0, scale: 0.96 }}
            transition={{ duration: 0.8, ease: "easeInOut" }}
            className="fixed inset-0 z-50 bg-[#F4F0F9] flex flex-col items-center justify-center p-6 text-center"
            id="intro-splash-overlay"
          >
            {/* Ambient background particles */}
            <div className="absolute inset-0 pointer-events-none overflow-hidden">
              <div className="absolute top-1/4 left-1/3 w-2.5 h-2.5 rounded-full bg-purple-300 blur-sm animate-ping" style={{ animationDuration: '3s' }} />
              <div className="absolute bottom-1/3 right-1/4 w-2 h-2 rounded-full bg-pink-300 blur-sm animate-ping" style={{ animationDuration: '4s' }} />
              <div className="absolute top-2/3 left-1/5 w-3.5 h-3.5 rounded-full bg-indigo-200 blur-[2px] opacity-65 animate-bounce" style={{ animationDuration: '5s' }} />
            </div>

            <motion.div 
              initial={{ scale: 0.8, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              transition={{ delay: 0.1, duration: 1, ease: "easeOut" }}
              className="relative z-10 flex flex-col items-center max-w-lg"
            >
              {/* Spinning Logo Intro */}
              <Logo size={150} animate={true} className="mb-6 drop-shadow-[0_0_30px_rgba(167,139,250,0.55)]" />

              <h1 className="text-4xl md:text-5xl font-black bg-gradient-to-r from-[#210833] via-[#5c248d] to-[#bf4080] bg-clip-text text-transparent tracking-tight mb-2 uppercase select-none">
                Yogasan Master
              </h1>
              
              <div className="h-[2px] w-40 bg-gradient-to-r from-transparent via-purple-300 to-transparent mb-4" />
              
              <p className="text-[#5c248d]/80 font-mono tracking-wider text-xs uppercase mb-8 font-bold">
                AI-POWERED PERSONALIZED YOGA SANCTUARY
              </p>

              <blockquote className="text-slate-600 italic text-sm font-serif px-6 mb-10 max-w-md relative line-clamp-3">
                “Yoga is the steady flight of attention toward the infinite stillness of being.”
              </blockquote>

              {/* Startup Live Action launcher Button */}
              <motion.button
                whileHover={{ scale: 1.05, boxShadow: "0 0 25px rgba(168,85,247,0.3)" }}
                whileTap={{ scale: 0.98 }}
                onClick={() => {
                  setShowIntro(false);
                  handleToggleSound("432"); // Start premium gentle yoga frequency instantly!
                }}
                className="relative group overflow-hidden px-8 py-4 bg-gradient-to-r from-purple-600 via-indigo-600 to-pink-500 rounded-full font-bold tracking-wider text-white shadow-xl shadow-purple-500/10 uppercase transition-all duration-300 flex items-center gap-2 cursor-pointer"
                id="open-yogasan-master-btn"
              >
                <span className="relative z-10 flex items-center gap-2">
                  Open Yogasan Master
                  <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
                </span>
                <span className="absolute inset-0 bg-gradient-to-r from-pink-500 via-purple-600 to-indigo-600 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
              </motion.button>

              <div className="mt-16 text-xs text-slate-500 font-medium">
                Developed for the Digital Soul by <span className="text-[#5c248d] font-bold font-serif">TASHI & Shubhi</span>
              </div>
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>

      {/* HEADER SECTION */}
      <header className="sticky top-0 z-40 bg-white/85 backdrop-blur-md border-b border-purple-100 px-4 py-3 lg:px-8 flex items-center justify-between shadow-md" id="header-bar">
        <div className="flex items-center gap-3 cursor-pointer" onClick={() => setActiveTab("home")}>
          <Logo size={42} animate={true} />
          <div>
            <h1 className="text-lg font-extrabold bg-gradient-to-r from-[#250d35] to-[#632997] bg-clip-text text-transparent uppercase tracking-wider">
              Yogasan Master
            </h1>
            <p className="text-[9px] font-mono tracking-widest text-[#EC4899] uppercase leading-none font-bold">
              AI Wellness Startup
            </p>
          </div>
        </div>

        {/* Desktop Nav links */}
        <nav className="hidden md:flex items-center gap-1 text-sm font-semibold">
          <button 
            onClick={() => { setActiveTab("home"); setIsPracticeMode(false); }} 
            className={`px-3 py-1.5 rounded-lg transition-all cursor-pointer ${activeTab === "home" ? "bg-purple-100/80 text-[#5c248d] border border-purple-200/50 font-bold" : "text-slate-600 hover:text-purple-800 hover:bg-purple-50/50"}`}
          >
            Home Plan
          </button>
          <button 
            onClick={() => { setActiveTab("assessment"); setIsPracticeMode(false); }} 
            className={`px-3 py-1.5 rounded-lg transition-all cursor-pointer ${activeTab === "assessment" ? "bg-purple-100/80 text-[#5c248d] border border-purple-200/50 font-bold" : "text-slate-600 hover:text-purple-800 hover:bg-purple-50/50"}`}
          >
            AI Assessment
          </button>
          <button 
            onClick={() => { setActiveTab("poses"); setIsPracticeMode(false); }} 
            className={`px-3 py-1.5 rounded-lg transition-all cursor-pointer ${activeTab === "poses" ? "bg-purple-100/80 text-[#5c248d] border border-purple-200/50 font-bold" : "text-slate-600 hover:text-purple-800 hover:bg-purple-50/50"}`}
          >
            Explore Poses
          </button>
          <button 
            onClick={() => { setActiveTab("tracker"); setIsPracticeMode(false); }} 
            className={`px-3 py-1.5 rounded-lg transition-all cursor-pointer ${activeTab === "tracker" ? "bg-purple-100/80 text-[#5c248d] border border-purple-200/50 font-bold" : "text-slate-600 hover:text-purple-800 hover:bg-purple-50/50"}`}
          >
            Tracker Dashboard
          </button>
          <button 
            onClick={() => { setActiveTab("cute"); setIsPracticeMode(false); }} 
            className={`px-3 py-1.5 rounded-lg transition-all cursor-pointer ${activeTab === "cute" ? "bg-pink-100/80 text-pink-700 border border-pink-200/50 font-bold" : "text-slate-600 hover:text-pink-700 hover:bg-pink-50/50"}`}
          >
            🥗 Cooking & Pet
          </button>
          <button 
            onClick={() => { setActiveTab("jokes"); setIsPracticeMode(false); }} 
            className={`px-3 py-1.5 rounded-lg transition-all cursor-pointer ${activeTab === "jokes" ? "bg-pink-100/80 text-pink-700 border border-pink-200/50 font-bold" : "text-slate-600 hover:text-pink-700 hover:bg-pink-50/50"}`}
          >
            😂 Jokes Page
          </button>
        </nav>

        {/* Soundtrack Synthesizer Dashboard Console */}
        <div className="flex items-center gap-3 bg-purple-100/70 rounded-full border border-purple-200/50 py-1.5 px-3 md:px-4 shadow-sm" id="audio-dashboard">
          <div className="hidden lg:flex items-center gap-1 text-[11px] font-mono text-purple-800 mr-2 border-r border-purple-200 pr-3">
            <Music className="w-3.5 h-3.5 text-[#EC4899] shrink-0 animate-spin" style={{ animationDuration: "12s" }} />
            <span>ambient sound:</span>
            <select
              value={soundFrequency}
              onChange={(e) => handleFrequencyChange(e.target.value as any)}
              className="bg-transparent text-[#5c248d] border-none outline-none font-bold cursor-pointer hover:text-pink-600"
            >
              <option value="432" className="bg-white text-slate-800">🧘 432Hz Serene Melodies</option>
              <option value="528" className="bg-white text-slate-800">✨ 528Hz Golden Chimes</option>
              <option value="639" className="bg-white text-slate-800">💚 639Hz Heart Harmony</option>
            </select>
          </div>

          <div className="flex items-center gap-2">
            <button
              onClick={() => handleToggleSound()}
              className="p-1.5 rounded-full hover:bg-purple-100 transition-all text-[#5c248d] hover:text-pink-600 cursor-pointer"
              title="Toggle Ambient Yoga Music & Solfeggio Chimes"
            >
              {soundActive ? (
                <div className="flex items-center gap-1">
                  <Volume2 className="w-4 h-4 text-pink-600 animate-bounce" />
                  <span className="text-[9px] font-mono text-pink-600 animate-pulse hidden xs:inline uppercase font-bold">MUTE</span>
                </div>
              ) : (
                <div className="flex items-center gap-1">
                  <VolumeX className="w-4 h-4 text-slate-500" />
                  <span className="text-[9px] font-mono text-slate-500 hidden xs:inline uppercase font-bold">PLAY YOGA MUSIC</span>
                </div>
              )}
            </button>
            
            {soundActive && (
              <div className="flex items-center gap-1 w-12 md:w-16">
                <input
                  type="range"
                  min="0.01"
                  max="0.4"
                  step="0.01"
                  value={soundVolume}
                  onChange={(e) => setSoundVolume(Number(e.target.value))}
                  className="w-full h-1 bg-purple-200 rounded-lg appearance-none cursor-pointer accent-[#EC4899]"
                  title="Music Volume Level"
                />
              </div>
            )}
          </div>
        </div>
      </header>

      {/* HEADER DAILY MOTIVATIONAL BANNER */}
      <section className="bg-[#FAF5FF]/90 border-b border-purple-100 py-2.5 px-4 z-10 text-center relative pointer-events-auto">
        <div className="max-w-4xl mx-auto flex flex-col md:flex-row items-center justify-center gap-2">
          <span className="inline-flex items-center gap-1 text-[11px] font-mono bg-pink-100 text-[#EC4899] uppercase px-2 py-0.5 rounded-full border border-pink-200 font-bold">
            <Sparkles className="w-3 h-3 text-yellow-500 animate-spin" /> Wisdom of the Day
          </span>
          <p className="text-xs text-slate-700 font-medium italic transition-all duration-1000">
            “{DAILY_QUOTES[quoteIndex].text}”
          </p>
          <span className="text-[10px] text-[#5c248d] font-semibold font-serif">
            — {DAILY_QUOTES[quoteIndex].author}
          </span>
        </div>
      </section>

      {/* MAIN LAYOUT */}
      <main className="flex-grow z-10 p-4 lg:p-8 max-w-7xl mx-auto w-full flex flex-col gap-8">
        
        {/* ACTIVE PRACTICE ROOM (OVERLAY / SUB-SCREEN STATE) */}
        <AnimatePresence>
          {isPracticeMode && curatedPlan && (
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              exit={{ opacity: 0, y: -30 }}
              className="bg-white/90 backdrop-blur-md rounded-3xl border border-purple-200 p-4 md:p-8 shadow-2xl relative overflow-hidden"
              id="active-practice-room"
            >
              <div className="absolute inset-0 bg-gradient-to-b from-purple-500/5 to-transparent pointer-events-none" />

              {/* Close session button */}
              <button
                onClick={() => setIsPracticeMode(false)}
                className="absolute top-4 right-4 p-2.5 rounded-full bg-purple-50 border border-purple-200 hover:bg-purple-100 text-[#5c248d] cursor-pointer z-20 transition-all font-bold"
                title="Exit Practice Room"
              >
                <X className="w-5 h-5" />
              </button>

              {!practiceCompleted ? (
                <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 relative z-10">
                  
                  {/* Left Column: Pose Visualizer and Audio Assist */}
                  <div className="lg:col-span-5 flex flex-col items-center justify-center space-y-4">
                    <span className="text-[10px] uppercase font-mono tracking-widest text-pink-600 bg-pink-100/80 p-2 py-0.5 rounded-full border border-pink-200/50 font-bold">
                      Pose {activePoseIdx + 1} of {curatedPlan.poses.length}
                    </span>
                    
                    <h3 className="text-3xl font-black text-center text-[#210833]">
                      {curatedPlan.poses[activePoseIdx].name}
                    </h3>
                    <p className="text-sm font-semibold italic text-[#5c248d] font-serif -mt-2">
                      {curatedPlan.poses[activePoseIdx].sanskritName}
                    </p>

                    {/* Highly crafted physical form model graphic visualizer */}
                    <PoseVisualizer 
                      poseId={curatedPlan.poses[activePoseIdx].id} 
                      isBreathing={timerRunning} 
                      size={280}
                      className="w-full max-w-[340px] aspect-square rounded-2xl p-6"
                    />

                    {/* Explain / Speak assist bubble trigger */}
                    <div className="w-full max-w-[340px] flex flex-col gap-2">
                       <button
                        onClick={() => triggerVoiceExplanation(curatedPlan.poses[activePoseIdx])}
                        className="w-full py-2 bg-purple-50 border border-purple-200/80 text-[#5c248d] hover:text-[#210833] rounded-xl text-xs font-bold hover:bg-purple-100/80 transition-all flex items-center justify-center gap-1.5 cursor-pointer"
                      >
                        <Brain className="w-4 h-4 text-pink-500" />
                        {voiceExplanation ? "Collapse Vocal Guide" : "Explain Alignment Steps"}
                      </button>

                      {voiceExplanation && (
                        <motion.div
                          initial={{ opacity: 0, scale: 0.95 }}
                          animate={{ opacity: 1, scale: 1 }}
                          className="bg-white border border-pink-200 rounded-xl p-3 text-xs text-slate-700 text-center leading-relaxed font-serif shadow-sm"
                        >
                          {voiceExplanation}
                        </motion.div>
                      )}
                    </div>
                  </div>

                  {/* Right Column: Timer, Steps, Guidelines */}
                  <div className="lg:col-span-7 flex flex-col justify-between space-y-6">
                    
                    {/* Upper Row: Real-time dynamic timer clock */}
                    <div className="bg-purple-50/70 rounded-2xl p-6 border border-purple-100 flex flex-col md:flex-row items-center justify-between gap-6">
                      <div className="flex flex-col text-center md:text-left">
                        <span className="text-xs uppercase font-mono tracking-wider text-slate-500 font-bold">Duration Needed</span>
                        <span className="text-2xl font-extrabold text-[#210833]">{curatedPlan.poses[activePoseIdx].duration}</span>
                      </div>

                      {/* Circular styled clock */}
                      <div className="relative flex items-center justify-center w-28 h-28 bg-purple-100/40 rounded-full ring-4 ring-pink-500/10 shadow-lg">
                        <div className="flex flex-col items-center">
                          <span className="text-3xl font-black font-mono text-[#210833] tracking-tighter">
                            {Math.floor(timeRemaining / 60)}:{(timeRemaining % 60).toString().padStart(2, "0")}
                          </span>
                          <span className="text-[9px] uppercase font-mono tracking-widest text-pink-600 -mt-1 font-bold">
                            {timerRunning ? "BREATHE" : "PAUSED"}
                          </span>
                        </div>
                      </div>

                      {/* Controls Buttons */}
                      <div className="flex items-center gap-2">
                        <button
                          onClick={() => setTimerRunning(!timerRunning)}
                          className="p-3 bg-[#7c3aed] hover:bg-[#6d28d9] text-white rounded-full shadow-lg cursor-pointer transition-all hover:scale-105"
                          title={timerRunning ? "Pause" : "Play"}
                        >
                          {timerRunning ? <Pause className="w-5 h-5" /> : <Play className="w-5 h-5 fill-white" />}
                        </button>
                        
                        <button
                          onClick={() => setTimeRemaining(curatedPlan.poses[activePoseIdx].durationSeconds)}
                          className="p-3 bg-white border border-purple-200 hover:bg-purple-50 text-slate-700 rounded-full cursor-pointer transition-all shadow-sm"
                          title="Reset Timer"
                        >
                          <RotateCcw className="w-5 h-5" />
                        </button>

                        <button
                          onClick={skipToNextPose}
                          className="px-4 py-2 bg-purple-100 border border-purple-200 hover:bg-purple-200/80 text-[#5c248d] rounded-full font-bold text-xs flex items-center gap-1 cursor-pointer"
                        >
                          Next Pose <ChevronRight className="w-4 h-4" />
                        </button>
                      </div>
                    </div>

                    {/* Mid Section: Step-by-Step execution instruction blocks */}
                    <div className="space-y-4">
                      <h4 className="text-sm uppercase font-mono tracking-widest text-pink-600 font-bold">
                        Alignment Steps (Follow Closely)
                      </h4>
                      <ol className="space-y-2 text-slate-700 text-sm">
                        {curatedPlan.poses[activePoseIdx].steps.map((step, idx) => (
                          <li key={idx} className="flex gap-3 bg-white p-3 rounded-xl border border-purple-100 shadow-sm">
                            <span className="w-5 h-5 shrink-0 rounded-full bg-purple-100 flex items-center justify-center text-xs font-mono font-bold text-[#5c248d]">
                              {idx + 1}
                            </span>
                            <p className="leading-relaxed text-slate-700 font-medium">{step}</p>
                          </li>
                        ))}
                      </ol>
                    </div>

                    {/* Bottom Row: Core benefits & safety warnings */}
                    <div className="grid grid-cols-1 md:grid-cols-2 gap-4 pt-2">
                      <div className="bg-emerald-50 border border-emerald-200/50 p-4 rounded-xl">
                        <h5 className="text-xs font-bold text-emerald-800 uppercase font-mono tracking-wider flex items-center gap-1.5 mb-2">
                          <Check className="w-4 h-4 text-emerald-600" /> Key Biomechanical Benefits
                        </h5>
                        <ul className="text-xs text-slate-600 space-y-1.5">
                          {curatedPlan.poses[activePoseIdx].benefits.map((b, i) => (
                            <li key={i} className="list-disc list-inside leading-relaxed">{b}</li>
                          ))}
                        </ul>
                      </div>

                      <div className="bg-amber-50 border border-amber-200/50 p-4 rounded-xl">
                        <h5 className="text-xs font-bold text-amber-800 uppercase font-mono tracking-wider flex items-center gap-1.5 mb-2">
                          <ShieldAlert className="w-4 h-4 text-amber-600" /> Safety Warnings & Limits
                        </h5>
                        <ul className="text-xs text-slate-600 space-y-1.5">
                          {curatedPlan.poses[activePoseIdx].precautions.map((p, i) => (
                            <li key={i} className="list-disc list-inside leading-relaxed">{p}</li>
                          ))}
                        </ul>
                      </div>
                    </div>

                  </div>

                </div>
              ) : (
                /* Session finished congratulations block */
                <div className="text-center py-12 max-w-lg mx-auto relative z-10 flex flex-col items-center">
                  <div className="w-20 h-20 bg-purple-100 rounded-full border border-purple-200/80 flex items-center justify-center mb-6 animate-bounce">
                    <Award className="w-10 h-10 text-pink-500" />
                  </div>

                  <h3 className="text-4xl font-extrabold text-[#210833] uppercase tracking-tight mb-2">
                    Divine Session Complete!
                  </h3>
                  
                  <p className="text-[#5c248d] font-mono text-xs uppercase mb-6 tracking-widest font-bold">
                    Namaste {profile.name}
                  </p>

                  <div className="bg-purple-50/50 border border-purple-200/80 rounded-2xl p-6 w-full space-y-4 mb-8">
                    <p className="text-slate-700 text-sm italic font-serif leading-relaxed">
                      “{curatedPlan.mindsetQuote}”
                    </p>
                    
                    <div className="h-[1px] bg-purple-100" />

                    <div className="grid grid-cols-2 gap-4">
                      <div className="text-center">
                        <span className="block text-[10px] font-mono uppercase tracking-wider text-slate-500 font-bold">Streak Maintained</span>
                        <span className="text-xl font-bold text-[#210833] flex items-center justify-center gap-1 mt-1">
                          <Flame className="w-5 h-5 text-pink-600 fill-pink-600" /> {streakCount} Days
                        </span>
                      </div>
                      <div className="text-center">
                        <span className="block text-[10px] font-mono uppercase tracking-wider text-slate-500 font-bold">Est. Burned Weight</span>
                        <span className="text-xl font-bold text-[#210833] flex items-center justify-center gap-1 mt-1">
                          <Flame className="w-5 h-5 text-orange-500" /> {curatedPlan.totalCalories} kcal
                        </span>
                      </div>
                    </div>
                  </div>

                  <div className="flex flex-col sm:flex-row gap-3 w-full justify-center">
                    <button
                      onClick={restartPracticeSession}
                      className="px-6 py-3 bg-purple-600 hover:bg-purple-500 text-white rounded-xl font-bold text-sm cursor-pointer transition-all flex items-center justify-center gap-1.5 shadow-lg shadow-purple-200/55"
                    >
                      <RotateCcw className="w-4 h-4" /> Practice Again
                    </button>
                    <button
                      onClick={() => { setIsPracticeMode(false); setActiveTab("tracker"); }}
                      className="px-6 py-3 bg-slate-900 hover:bg-slate-800 text-white rounded-xl font-bold text-sm cursor-pointer transition-all"
                    >
                      View Live Tracker
                    </button>
                  </div>
                </div>
              )}
            </motion.div>
          )}
        </AnimatePresence>

        {/* HOME VIEW: HERO & AI SCREEN */}
        {activeTab === "home" && !isPracticeMode && (
          <div className="space-y-12">
            
            {/* NOVELTY ALERT BANNER FOR HEALTHY RECIPES & FUN */}
            <motion.div 
              initial={{ scale: 0.95, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              whileHover={{ scale: 1.01 }}
              transition={{ type: "spring", stiffness: 300, damping: 20 }}
              onClick={() => { setActiveTab("cute"); playSuccessFanfare(); }}
              className="bg-gradient-to-r from-pink-500 via-purple-600 to-indigo-600 p-[3px] rounded-3xl shadow-xl cursor-pointer group hover:shadow-2xl transition-all"
            >
              <div className="bg-white rounded-[21px] p-5 flex flex-col sm:flex-row items-center gap-4 text-center sm:text-left justify-between">
                <div className="flex items-center gap-4 flex-col sm:flex-row">
                  <span className="text-4xl animate-bounce shrink-0 select-none">🥑🥗</span>
                  <div>
                    <h4 className="text-base font-extrabold text-slate-800 group-hover:text-pink-650 transition-colors uppercase tracking-tight flex items-center justify-center sm:justify-start gap-1.5">
                      Healthy Recipes & Funny Puns found here! <span className="bg-pink-105 text-pink-705 text-[9px] font-bold px-2 py-0.5 rounded-full uppercase scale-90">Hot News</span>
                    </h4>
                    <p className="text-xs text-slate-600 font-semibold mt-1 leading-relaxed">
                      Looking for delicious Ayurvedic food secrets and funny yoga puns? We cooked them inside the new <strong className="text-purple-700 underline font-extrabold">🥗 Healthy & Funny Hub</strong>. Tap anywhere on this card to check them out!
                    </p>
                  </div>
                </div>
                <div className="shrink-0 scale-95 sm:scale-100">
                  <span className="inline-flex items-center gap-1.5 text-[11px] font-mono tracking-wider text-pink-700 bg-pink-100 border border-pink-200 px-4 py-2 rounded-2xl font-black uppercase group-hover:bg-pink-500 group-hover:text-white transition-all shadow-sm">
                    Let's Cook! 👩‍🍳 <ChevronRight className="w-3.5 h-3.5" />
                  </span>
                </div>
              </div>
            </motion.div>
            
            {/* HERO SECTION CONTAINER */}
            <section className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center bg-white/80 backdrop-blur-md p-6 md:p-12 rounded-3xl border border-purple-100 shadow-md relative overflow-hidden" id="hero-banner">
              <div className="absolute inset-0 bg-gradient-to-tr from-purple-500/5 to-transparent pointer-events-none" />
              <div className="absolute bottom-[-100px] left-[-100px] w-96 h-96 rounded-full bg-pink-100/30 blur-[120px] pointer-events-none" />
              
              {/* Left Column Text & CTAs */}
              <div className="lg:col-span-11 xl:col-span-7 space-y-6 text-center lg:text-left relative z-10">
                <span className="inline-flex items-center gap-1.5 text-[11px] font-mono uppercase tracking-widest text-[#be185d] bg-pink-50 px-3 py-1 rounded-full border border-pink-200/60 font-bold animate-pulse">
                  <Sparkles className="w-3.5 h-3.5 text-yellow-500 hover:rotate-45 transition-transform" /> Spiritual Intelligence Is Born
                </span>

                <h1 className="text-4xl md:text-5xl lg:text-6xl font-black leading-tight tracking-tight uppercase text-slate-900" id="hero-heading">
                  Welcome to <br />
                  <span className="bg-gradient-to-r from-purple-700 to-[#9d174d] bg-clip-text text-transparent underline decoration-pink-450/40">
                    Yogasan Master
                  </span>
                </h1>

                <p className="text-sm md:text-base text-slate-650 leading-relaxed max-w-xl mx-auto lg:mx-0 font-medium">
                  AI-Powered Personalized Yoga for Every Age & Flexibility Level. 
                  Acharya, your digital AI teacher, leverages ancient Patanjali alignment values with contemporary sports science to curate metabolic, therapeutic, and mindful routines safely.
                </p>

                {/* CTAs */}
                <div className="flex flex-col sm:flex-row items-center justify-center lg:justify-start gap-4 pt-2">
                  <button
                    onClick={() => setActiveTab("assessment")}
                    className="w-full sm:w-auto px-8 py-3.5 bg-gradient-to-r from-purple-600 to-indigo-600 hover:from-purple-500 hover:to-indigo-500 text-white rounded-xl font-bold tracking-wider shadow-lg shadow-purple-200/50 hover:shadow-purple-300 uppercase cursor-pointer"
                  >
                    Start Your Journey
                  </button>
                  
                  <button
                    onClick={() => {
                      const chatSec = document.getElementById("acharya-chat-section");
                      chatSec?.scrollIntoView({ behavior: "smooth", block: "start" });
                    }}
                    className="w-full sm:w-auto px-6 py-3.5 bg-purple-50 hover:bg-purple-100 border border-purple-200 text-[#5c248d] rounded-xl font-bold uppercase transition-all cursor-pointer"
                  >
                    Talk to AI Teacher
                  </button>

                  <button
                    onClick={() => {
                      const poseSec = document.getElementById("pose-explorer-section");
                      poseSec?.scrollIntoView({ behavior: "smooth", block: "start" });
                    }}
                    className="w-full sm:w-auto px-6 py-3.5 bg-white hover:bg-purple-50/50 border border-purple-200 text-slate-700 hover:text-[#5c248d] rounded-xl font-bold text-xs uppercase cursor-pointer transition-all"
                  >
                    Explore Yoga Plans
                  </button>
                </div>

                {/* Micro branding tagline */}
                <div className="pt-4 flex items-center justify-center lg:justify-start gap-2 text-xs text-slate-650 font-medium">
                  <Award className="w-4 h-4 text-pink-600 shrink-0" />
                  <span>Real-time local sound synthesis at <strong className="text-[#5c248d] font-mono font-bold">432Hz Solfeggio</strong> carrier frequency enabled.</span>
                </div>
              </div>

              {/* Right Column: Giant Animated Yoga Pose / Mind Particle Sphere */}
              <div className="lg:col-span-12 xl:col-span-5 flex items-center justify-center relative z-10 pt-6 lg:pt-0">
                <div className="relative w-full max-w-[320px]">
                  {/* Outer spinning orb line */}
                  <div className="absolute inset-x-0 inset-y-0 border-2 border-indigo-500/20 border-dashed rounded-full animate-spin [animation-duration:30s] pointer-events-none" />
                  <div className="absolute inset-5 border border-pink-500/10 rounded-full animate-spin [animation-duration:15s] pointer-events-none" />
                  
                  {/* Calming, premium animated silhouette */}
                  <PoseVisualizer 
                    poseId="sukhasana" 
                    isBreathing={true} 
                    size={270}
                    className="bg-transparent border-none p-0 drop-shadow-[0_0_25px_rgba(147,51,234,0.3)]"
                  />
                </div>
              </div>

            </section>

            {/* CURATED / RECOMMENDATION VIEW DASHBOARD */}
            {curatedPlan && (
              <section className="bg-white rounded-3xl border border-purple-100 p-6 md:p-8 scroll-mt-24 shadow-md" id="recommended-section">
                
                <div className="flex flex-col md:flex-row items-start md:items-center justify-between gap-4 mb-6 border-b border-purple-100 pb-6">
                  <div>
                    <span className="text-[10px] uppercase font-mono tracking-widest text-[#be185d] bg-pink-50 p-2 py-0.5 rounded-full border border-pink-200/50 font-bold">
                      Prescribed Path (Generated dynamically: {planSource})
                    </span>
                    <h2 className="text-3xl font-extrabold text-[#210833] mt-1">
                      {curatedPlan.title}
                    </h2>
                    <p className="text-[#5c248d]/85 font-medium text-sm mt-1 max-w-xl">
                      {curatedPlan.tagline}
                    </p>
                  </div>

                  <div className="flex items-center gap-2">
                    <button
                      onClick={startPracticeRoom}
                      className="px-6 py-3 bg-gradient-to-r from-purple-600 via-[#7c3aed] to-pink-500 hover:opacity-95 text-white rounded-xl font-extrabold text-sm shadow-lg shadow-purple-200/40 cursor-pointer flex items-center gap-1.5 uppercase transition-all"
                    >
                      <Play className="w-4 h-4 fill-white animate-pulse" /> Start Session Now
                    </button>
                  </div>
                </div>

                {/* Summary Metadata card */}
                <div className="grid grid-cols-1 md:grid-cols-4 gap-4 mb-8">
                  <div className="bg-purple-50/50 border border-purple-100 p-4 rounded-xl flex items-center gap-3 shadow-sm">
                    <Timer className="w-8 h-8 text-indigo-600 hover:rotate-12 transition-transform" />
                    <div>
                      <span className="block text-[10px] font-mono text-slate-500 uppercase tracking-wider font-bold">Total Duration</span>
                      <strong className="text-lg text-[#210833] font-black">{curatedPlan.totalDuration}</strong>
                    </div>
                  </div>

                  <div className="bg-purple-50/50 border border-purple-100 p-4 rounded-xl flex items-center gap-3 shadow-sm">
                    <Flame className="w-8 h-8 text-[#EC4899] hover:animate-bounce" />
                    <div>
                      <span className="block text-[10px] font-mono text-slate-500 uppercase tracking-wider font-bold">Total Est. Calories</span>
                      <strong className="text-lg text-[#210833] font-black">{curatedPlan.totalCalories} kcal</strong>
                    </div>
                  </div>

                  <div className="bg-purple-50/50 border border-purple-100 p-4 rounded-xl flex items-center gap-3 shadow-sm">
                    <Award className="w-8 h-8 text-yellow-500" />
                    <div>
                      <span className="block text-[10px] font-mono text-slate-500 uppercase tracking-wider font-bold">Target Level</span>
                      <strong className="text-lg text-[#210833] font-black">{curatedPlan.difficulty} Badge</strong>
                    </div>
                  </div>

                  <div className="bg-purple-50/50 border border-purple-100 p-4 rounded-xl flex items-center gap-3 shadow-sm">
                    <Heart className="w-8 h-8 text-pink-500" />
                    <div>
                      <span className="block text-[10px] font-mono text-slate-500 uppercase tracking-wider font-bold">Mindset Focus</span>
                      <strong className="text-xs text-[#5c248d] font-bold font-serif leading-tight">Spiritual Union</strong>
                    </div>
                  </div>
                </div>

                {/* Sub Poses list inside prescription card */}
                <div className="space-y-4">
                  <h3 className="text-sm font-bold uppercase tracking-widest text-slate-500 font-mono">
                    Sequence & Choreography (Click standard Pose to view steps or explain)
                  </h3>
                  
                  <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                    {curatedPlan.poses.map((pose, pIndex) => (
                      <motion.div
                        key={pose.id + pIndex}
                        whileHover={{ y: -4, borderColor: 'rgba(168,85,247,0.4)', backgroundColor: '#FAF5FF' }}
                        className="bg-white border border-purple-100 p-4 rounded-2xl transition-all cursor-pointer select-none shadow-sm"
                        onClick={() => setSelectedPose(pose)}
                      >
                        <PoseVisualizer poseId={pose.id} isBreathing={false} size={110} className="mb-3 bg-purple-100/30 rounded-xl p-2" />
                        
                        <div className="flex items-center justify-between">
                          <span className="text-[9px] font-mono font-bold text-[#be185d] bg-pink-50 px-2 py-0.5 rounded-full border border-pink-100">
                            Step {pIndex + 1}
                          </span>
                          <span className="text-xs text-slate-500 font-bold">
                            {pose.duration}
                          </span>
                        </div>

                        <h4 className="text-base font-extrabold text-[#210833] mt-1.5">
                          {pose.name}
                        </h4>
                        <p className="text-xs text-[#5c248d] italic font-semibold font-serif">
                          {pose.sanskritName}
                        </p>

                        <div className="text-[10px] font-medium text-slate-500 mt-2 line-clamp-2">
                          {pose.steps[0]}
                        </div>
                      </motion.div>
                    ))}
                  </div>
                </div>

                {/* Prescription Safety Board */}
                <div className="mt-8 bg-purple-50/70 border border-purple-200/50 p-4 rounded-2xl flex flex-col md:flex-row items-center gap-4">
                  <div className="w-10 h-10 shrink-0 bg-purple-100 rounded-full flex items-center justify-center text-pink-600">
                    <ShieldAlert className="w-5 h-5" />
                  </div>
                  <div>
                    <h5 className="text-xs font-mono font-bold uppercase text-[#210833] tracking-widest leading-none mb-1">
                      Acharya Care Safety Advice
                    </h5>
                    <p className="text-xs text-slate-700 leading-relaxed font-medium">
                      {curatedPlan.safetyAdvice} Adjust standard poses using block pillows or stable physical supports if you experience any lumbar restrictions.
                    </p>
                  </div>
                </div>

              </section>
            )}

            {/* INTERACTIVE AI YOGA TEACHER ASSISTANT PANEL */}
            <section className="bg-white rounded-3xl border border-purple-100 p-6 md:p-8 grid grid-cols-1 lg:grid-cols-12 gap-8 scroll-mt-24 shadow-md" id="acharya-chat-section">
              
              {/* Left Side: Avatar & Bio */}
              <div className="lg:col-span-12 xl:col-span-4 flex flex-col items-center justify-center p-6 bg-purple-50/50 border border-purple-100 rounded-2xl text-center relative overflow-hidden">
                <div className="absolute top-0 right-0 w-24 h-24 bg-pink-100/20 blur-2xl pointer-events-none" />
                
                {/* Custom glowing interactive aura avatar */}
                <div className="relative mb-4 flex items-center justify-center">
                  <div className="absolute -inset-1 rounded-full bg-gradient-to-tr from-purple-500 via-pink-400 to-indigo-300 blur-md opacity-75 animate-pulse" />
                  <div className="relative w-28 h-28 bg-white rounded-full border border-purple-200 flex items-center justify-center overflow-hidden shadow-sm">
                    <Logo size={105} animate={true} />
                  </div>
                </div>

                <h3 className="text-xl font-bold bg-gradient-to-r from-purple-900 to-[#5c248d] bg-clip-text text-transparent uppercase tracking-wider">
                  Wise Acharya
                </h3>
                <p className="text-[10px] font-mono tracking-widest text-[#be185d] uppercase font-bold leading-none mt-1">
                  Lead AI Yoga Instructor
                </p>

                <div className="h-[1px] bg-purple-200/50 w-3/4 my-4" />

                <p className="text-xs text-slate-650 leading-relaxed font-semibold">
                  Deep classical knowledge integrated with advanced kinesiology feedback. 
                  Inquire regarding posture variations, breathing timings, muscle alignment checks, or custom restrictions.
                </p>
              </div>

              {/* Right Side: Chat Dialog Interface */}
              <div className="lg:col-span-12 xl:col-span-8 flex flex-col h-[400px] border border-purple-100 rounded-2xl bg-purple-50/20 overflow-hidden">
                
                <div className="px-4 py-3 bg-purple-100/50 border-b border-purple-100 flex items-center justify-between">
                  <div className="flex items-center gap-2">
                    <span className="w-2.5 h-2.5 rounded-full bg-emerald-500 animate-ping" />
                    <span className="text-xs font-mono tracking-wider font-bold text-slate-705">Guru Status: Mindfully Awake</span>
                  </div>
                  <span className="text-[10px] font-mono text-slate-500 uppercase font-bold">Scriptures / Science Hybrid Node</span>
                </div>

                {/* Dialog Messages Window */}
                <div className="flex-grow overflow-y-auto p-4 space-y-4 font-normal text-sm bg-white/70">
                  {chatMessages.map((msg) => (
                    <div 
                      key={msg.id} 
                      className={`flex ${msg.sender === "user" ? "justify-end" : "justify-start"}`}
                    >
                      <div className={`max-w-[85%] rounded-2xl p-3.5 ${
                        msg.sender === "user" 
                          ? "bg-purple-650 text-white rounded-tr-none shadow-sm" 
                          : "bg-white border border-purple-100 text-slate-800 rounded-tl-none font-sans leading-relaxed shadow-sm font-medium"
                      }`}>
                        
                        {/* Sender branding label */}
                        <div className={`flex items-center gap-1.5 text-[9px] font-mono mb-1 leading-none uppercase ${msg.sender === "user" ? "text-purple-200" : "text-purple-700"}`}>
                          {msg.sender === "user" ? <User className="w-2.5 h-2.5" /> : <Brain className="w-2.5 h-2.5 text-pink-500" />}
                          <span>{msg.sender === "user" ? "Your Inquiry" : "Acharya Guidance"}</span>
                          <span className="text-slate-400 ml-auto font-bold">{msg.timestamp}</span>
                        </div>

                        <p className={`whitespace-pre-line leading-relaxed ${msg.sender === "user" ? "text-white" : "text-slate-700"}`}>{msg.text}</p>
                      </div>
                    </div>
                  ))}

                  {isChatTyping && (
                    <div className="flex justify-start">
                      <div className="bg-purple-100/60 border border-purple-200/50 rounded-2xl rounded-tl-none p-3 max-w-sm flex items-center gap-2 shadow-sm">
                        <span className="text-[10px] font-mono text-purple-700 font-bold">Acharya is preparing wisdom</span>
                        <div className="flex gap-1 animate-pulse">
                          <span className="w-1.5 h-1.5 bg-pink-500 rounded-full animate-bounce" style={{ animationDelay: '0ms' }} />
                          <span className="w-1.5 h-1.5 bg-purple-500 rounded-full animate-bounce" style={{ animationDelay: '150ms' }} />
                          <span className="w-1.5 h-1.5 bg-indigo-500 rounded-full animate-bounce" style={{ animationDelay: '300ms' }} />
                        </div>
                      </div>
                    </div>
                  )}

                  <div ref={chatEndRef} />
                </div>

                {/* Dialog Control Form */}
                <div className="p-3 border-t border-purple-100 bg-white flex items-center gap-2 shadow-inner">
                  <input
                    type="text"
                    value={chatInput}
                    onChange={(e) => setChatInput(e.target.value)}
                    onKeyDown={(e) => { if (e.key === "Enter") handleSendChatMessage(); }}
                    placeholder="Ask Acharya: 'I have stiff hamstrings', or 'explain mountain pose'..."
                    className="flex-grow bg-[#FAF8FE] border border-purple-200 rounded-xl px-4 py-2.5 text-xs text-slate-800 placeholder-slate-400 focus:outline-none focus:border-purple-400 focus:ring-1 focus:ring-purple-400/20"
                    id="chat-user-input"
                  />
                  <button
                    onClick={handleSendChatMessage}
                    className="p-2.5 bg-purple-600 hover:bg-purple-500 hover:scale-105 transition-all text-white rounded-xl shadow-md cursor-pointer shrink-0"
                    title="Transmit Message"
                  >
                    <Send className="w-4 h-4" />
                  </button>
                </div>

              </div>

            </section>

            {/* FEATURES AND BENFITS GRID CARD SECTION */}
            <section className="space-y-6">
              <div className="text-center">
                <span className="text-[#be185d] font-mono text-xs uppercase tracking-widest font-bold">Uncompromising Quality</span>
                <h2 className="text-3xl font-black text-[#210833] uppercase tracking-tight mt-1">
                  Yogasan Wellness Capabilities
                </h2>
              </div>

              <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                {[
                  {
                    id: "feat1",
                    title: "AI Yoga Guidance",
                    desc: "Interactive biomechanical steps adapted on the fly based on your customized user metrics and metabolic goals.",
                    icon: Sparkles,
                    color: "text-amber-700 bg-amber-150/60"
                  },
                  {
                    id: "feat2",
                    title: "Personalized Plans",
                    desc: "Lifespan yoga engineering for kids, adults, active athletes, and restorative geriatric chair yoga sets.",
                    icon: Compass,
                    color: "text-indigo-700 bg-indigo-150/60"
                  },
                  {
                    id: "feat3",
                    title: "Meditation Sessions",
                    desc: "Pure Web Audio Solfeggio carrier waves layered with simulated cyclical diaphragmatic ocean breath guidelines.",
                    icon: Brain,
                    color: "text-purple-700 bg-purple-150/60"
                  },
                  {
                    id: "feat4",
                    title: "Flexibility Tracking",
                    desc: "Sophisticated streak calculators paired with calorie ring indicators to visualize your physical progression.",
                    icon: TrendingUp,
                    color: "text-emerald-700 bg-emerald-150/60"
                  }
                ].map((item) => (
                  <div key={item.id} className="bg-white border border-purple-100 p-5 rounded-2xl relative overflow-hidden group shadow-sm transition-all hover:border-purple-200">
                    <div className="absolute top-0 right-0 w-16 h-16 bg-purple-600/5 blur-xl pointer-events-none group-hover:bg-purple-600/10 transition-all" />
                    <div className={`w-10 h-10 rounded-xl flex items-center justify-center mb-4 ${item.color} shadow-sm font-bold`}>
                      <item.icon className="w-5 h-5 shrink-0" />
                    </div>
                    <h3 className="text-base font-extrabold text-[#210833] uppercase tracking-wider mb-2">{item.title}</h3>
                    <p className="text-xs text-slate-600 leading-relaxed font-sans font-semibold">{item.desc}</p>
                  </div>
                ))}
              </div>
            </section>

            {/* CLASS COMPILATION LISTING SECTION */}
            <section className="bg-white p-6 md:p-8 rounded-3xl border border-purple-100 scroll-mt-24 shadow-md" id="pose-explorer-section">
              <div className="flex flex-col md:flex-row items-start md:items-center justify-between gap-4 mb-6">
                <div>
                  <h2 className="text-2xl font-black text-[#210833] uppercase tracking-tight">
                    Interactive Pose Demo
                  </h2>
                  <p className="text-xs text-slate-550 font-medium">
                    Interact directly with any pose below to investigate steps, precautions, and trigger high-fidelity AI-synthesized feedback!
                  </p>
                </div>
                <button
                  onClick={() => setActiveTab("poses")}
                  className="px-4 py-2 bg-purple-50 border border-purple-150 hover:bg-purple-100 text-[#5c248d] rounded-xl font-bold text-xs uppercase cursor-pointer"
                >
                  Explore All Categories <ChevronRight className="w-4 h-4 ml-1 inline" />
                </button>
              </div>

              <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-4">
                {Object.values(PRESET_POSES).slice(0, 5).map((pose) => (
                  <motion.div
                    key={pose.id}
                    whileHover={{ scale: 1.02 }}
                    className="bg-purple-50/20 border border-purple-100 p-4 rounded-xl flex flex-col justify-between cursor-pointer shadow-sm hover:border-purple-200"
                    onClick={() => setSelectedPose(pose)}
                  >
                    <div>
                      <PoseVisualizer poseId={pose.id} isBreathing={false} size={90} className="bg-purple-100/30 rounded-lg mb-3 p-2" />
                      <div className="text-[10px] font-mono text-purple-700 font-bold uppercase">{pose.sanskritName}</div>
                      <h4 className="text-base font-bold text-[#210833] mt-1">{pose.name}</h4>
                      <p className="text-xs text-slate-600 leading-relaxed font-sans line-clamp-2 mt-2 font-medium">{pose.steps[0]}</p>
                    </div>
                    
                    <div className="border-t border-purple-100 pt-3 mt-4 flex items-center justify-between text-[11px] font-bold text-slate-500">
                      <span>{pose.duration}</span>
                      <span className="text-pink-600">{pose.calories} kcal</span>
                    </div>
                  </motion.div>
                ))}
              </div>
            </section>

            {/* TESTIMONIALS SECTIONS */}
            <section className="space-y-6">
              <div className="text-center">
                <span className="text-pink-600 font-mono text-xs uppercase tracking-widest font-bold">Community Sanctuaries</span>
                <h2 className="text-3xl font-black text-[#210833] uppercase tracking-tight mt-1">
                  Yogasan Pathfinders
                </h2>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                {[
                  {
                    id: "t1",
                    name: "Shruti Aggarwal",
                    role: "Spinal Patient rehabilitation",
                    text: "“The dynamic spine decompressing routines Acharya curated solved my persistent lower back soreness from long software code cycles within two weeks of daily meditative flow. Deep gratitude to Shubhi and Tashi!”",
                    rating: 5,
                    avatar: "🕉️"
                  },
                  {
                    id: "t2",
                    name: "Naveen Agrawal",
                    role: "Adult Pranayama practitioner",
                    text: "“The customizable Solfeggio carrier soundtrack at 432Hz is brilliant! Having physical poses visualized in slow-breathing animations brings the peacefulness of a high-end yoga retreat directly to my desktop screen.”",
                    rating: 5,
                    avatar: "🧘‍♂️"
                  },
                  {
                    id: "t3",
                    name: "Ananda Dev",
                    role: "Advanced Vinyasa Yogi",
                    text: "“Unlike simple static workout tools, Yogasan Master understands biomechanical constraints perfectly. It recommended different modifications corresponding to my exact flexibility and age parameters.”",
                    rating: 5,
                    avatar: "🧘‍♀️"
                  }
                ].map((item) => (
                  <div key={item.id} className="bg-white border border-purple-100 rounded-2xl p-6 relative flex flex-col justify-between shadow-sm">
                    <p className="text-xs text-slate-650 leading-relaxed font-serif italic mb-6 font-medium">{item.text}</p>
                    
                    <div className="flex items-center gap-3">
                      <div className="w-10 h-10 rounded-full bg-purple-50 border border-purple-150 flex items-center justify-center text-xl shadow-sm">
                        {item.avatar}
                      </div>
                      <div>
                        <strong className="block text-xs font-bold text-slate-900 uppercase tracking-wider">{item.name}</strong>
                        <span className="text-[10px] text-purple-700 font-bold">{item.role}</span>
                      </div>
                      <div className="ml-auto text-xs font-bold text-amber-500">⭐⭐⭐⭐⭐</div>
                    </div>
                  </div>
                ))}
              </div>
            </section>

            {/* PRICING PLANS */}
            <section className="bg-white rounded-3xl border border-purple-100 p-6 md:p-8 scroll-mt-24 shadow-md">
              <div className="text-center max-w-lg mx-auto space-y-2 mb-8">
                <span className="text-pink-600 font-mono text-xs uppercase tracking-widest font-bold">Tier Prescriptions</span>
                <h3 className="text-3xl font-black text-[#210833] uppercase tracking-tight">Sanctuary Subscriptions</h3>
                <p className="text-xs text-slate-550 font-medium">
                  Begin for free, unlock divine advanced biomechanics and infinite scripture coaching when ready.
                </p>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto">
                {[
                  {
                    title: "Baseline Free",
                    price: "₹0",
                    period: "forever",
                    desc: "Perfect baseline path for yoga enthusiasts to get calculated metrics.",
                    features: ["Dynamic Assessment Routing", "4 default Yoga Prescriptions", "432Hz Core Carrier Sound", "Wise Acharya offline dialogue"],
                    btnText: "Embrace Flow",
                    hot: false
                  },
                  {
                    title: "Sadhaka Pro",
                    price: "₹399",
                    period: "month",
                    desc: "Infinite custom AI-curated plans optimized with Google Gemini models.",
                    features: ["Full Live Gemini Plan assessing", "Infinite detailed scriptures dialogue", "528Hz & 639Hz carrier frequencies", "Chakra Visual guides"],
                    btnText: "Elevate Practice",
                    hot: true
                  },
                  {
                    title: "Master Guru",
                    price: "₹999",
                    period: "month",
                    desc: "Direct biomechanical checkups, streak trackers, and complete offline access.",
                    features: ["Premium custom timer sound cues", "Direct clinical postures correction", "Interactive history charts", "Private coaching sync"],
                    btnText: "Divine Union",
                    hot: false
                  }
                ].map((item, index) => (
                  <div 
                    key={index} 
                    className={`rounded-2xl p-6 border flex flex-col justify-between relative overflow-hidden shadow-sm ${
                      item.hot 
                        ? "bg-gradient-to-b from-purple-50/50 to-white border-pink-400 md:-translate-y-2" 
                        : "bg-white border-purple-100"
                    }`}
                  >
                    {item.hot && (
                      <span className="absolute top-3 right-3 bg-pink-600 text-white font-mono text-[9px] uppercase tracking-widest px-2.5 py-0.5 rounded-full font-extrabold animate-pulse">
                        Most Radiant Choice
                      </span>
                    )}

                    <div className="space-y-4">
                      <span className="text-[11px] font-mono text-purple-700 uppercase block font-bold">{item.title}</span>
                      
                      <div className="flex items-baseline gap-1">
                        <span className="text-3xl font-black text-[#210833]">{item.price}</span>
                        <span className="text-xs text-slate-550 font-bold">/{item.period}</span>
                      </div>

                      <p className="text-xs text-slate-600 font-semibold leading-relaxed">{item.desc}</p>
                      
                      <div className="h-[1px] bg-purple-100" />

                      <ul className="text-xs text-slate-605 space-y-2 pb-6 font-semibold">
                        {item.features.map((f, i) => (
                          <li key={i} className="flex items-center gap-2">
                            <span className="w-1.5 h-1.5 rounded-full bg-pink-500" />
                            <span>{f}</span>
                          </li>
                        ))}
                      </ul>
                    </div>

                    <button
                      onClick={() => alert(`Namaste 🙏. Subscriptions system successfully configured. The ${item.title} tier is active for demonstration.`)}
                      className={`w-full py-3.5 rounded-xl font-bold tracking-wider uppercase text-xs cursor-pointer ${
                        item.hot 
                          ? "bg-gradient-to-r from-pink-500 to-purple-600 text-white shadow-lg" 
                          : "bg-[#7c3aed] text-white hover:bg-[#6d28d9]"
                      }`}
                    >
                      {item.btnText}
                    </button>
                  </div>
                ))}
              </div>
            </section>

          </div>
        )}

        {/* ASSESSMENT MODULE FORM VIEW */}
        {activeTab === "assessment" && (
          <motion.section 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            className="max-w-2xl mx-auto bg-white rounded-3xl border border-purple-100 p-6 md:p-10 relative overflow-hidden shadow-md"
            id="assessment-wizard-panel"
          >
            <div className="absolute top-0 left-0 w-32 h-32 bg-pink-100/10 blur-2xl pointer-events-none" />
            
            <div className="text-center space-y-3 mb-8">
              <span className="text-xs uppercase font-mono tracking-widest text-[#be185d] bg-pink-50 px-3 py-1 rounded-full border border-pink-200/50 font-bold">
                Biomechanics Intake
              </span>
              <h2 className="text-3xl font-black text-[#210833] uppercase tracking-tight">
                Calculate Yogic Formula
              </h2>
              <p className="text-xs text-slate-600 max-w-md mx-auto leading-relaxed font-semibold">
                Provide your precise physical envelope details. Acharya compiles these parameters to safely avoid injuries and activate alignment.
              </p>
            </div>

            <form onSubmit={handleFormSubmit} className="space-y-6">
              
              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                {/* Name */}
                <div className="space-y-1.5">
                  <label className="text-xs font-mono font-bold uppercase tracking-wider text-[#3b1259]">Name or Initial</label>
                  <input
                    type="text"
                    required
                    value={formName}
                    onChange={(e) => setFormName(e.target.value)}
                    placeholder="Enter name"
                    className="w-full bg-purple-50/50 border border-purple-200 rounded-xl px-4 py-2.5 text-xs text-slate-800 placeholder-slate-400 focus:outline-none focus:border-purple-400 focus:bg-white focus:ring-1 focus:ring-purple-400/20"
                  />
                </div>

                {/* Age */}
                <div className="space-y-1.5">
                  <label className="text-xs font-mono font-bold uppercase tracking-wider text-[#3b1259]">Age (for safety routing)</label>
                  <input
                    type="number"
                    required
                    min="4"
                    max="110"
                    value={formAge}
                    onChange={(e) => setFormAge(Number(e.target.value))}
                    className="w-full bg-purple-50/50 border border-purple-200 rounded-xl px-4 py-2.5 text-xs text-slate-800 placeholder-slate-400 focus:outline-none focus:border-purple-400 focus:bg-white focus:ring-1 focus:ring-purple-400/20"
                  />
                </div>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                {/* Gender */}
                <div className="space-y-1.5">
                  <label className="text-xs font-mono font-bold uppercase tracking-wider text-[#3b1259]">Gender Identity</label>
                  <select
                    value={formGender}
                    onChange={(e) => setFormGender(e.target.value)}
                    className="w-full bg-[#FAF8FE] border border-purple-200 rounded-xl px-4 py-2.5 text-xs text-slate-800 focus:outline-none focus:border-purple-400 cursor-pointer font-bold"
                  >
                    <option value="Male">Male</option>
                    <option value="Female">Female</option>
                    <option value="Non-binary">Non-binary / GNC</option>
                  </select>
                </div>

                {/* Height */}
                <div className="space-y-1.5">
                  <label className="text-xs font-mono font-bold uppercase tracking-wider text-[#3b1259]">Height (cm)</label>
                  <input
                    type="number"
                    required
                    value={formHeight}
                    onChange={(e) => setFormHeight(Number(e.target.value))}
                    className="w-full bg-purple-50/50 border border-purple-200 rounded-xl px-4 py-2.5 text-xs text-slate-800 placeholder-slate-400 focus:outline-none focus:border-purple-400 focus:bg-white focus:ring-1 focus:ring-purple-400/20"
                  />
                </div>

                {/* Weight */}
                <div className="space-y-1.5">
                  <label className="text-xs font-mono font-bold uppercase tracking-wider text-[#3b1259]">Weight (kg)</label>
                  <input
                    type="number"
                    required
                    value={formWeight}
                    onChange={(e) => setFormWeight(Number(e.target.value))}
                    className="w-full bg-purple-50/50 border border-purple-200 rounded-xl px-4 py-2.5 text-xs text-slate-800 placeholder-slate-400 focus:outline-none focus:border-purple-400 focus:bg-white focus:ring-1 focus:ring-purple-400/20"
                  />
                </div>
              </div>

              {/* Goal Grid */}
              <div className="space-y-2">
                <label className="text-xs font-mono font-bold uppercase tracking-wider text-[#3b1259] block">
                  Select Yogic Sanctuary Goal
                </label>
                <div className="grid grid-cols-2 md:grid-cols-3 gap-2">
                  {[
                    "Weight Loss", "Stress Relief", "Meditation", 
                    "Flexibility", "Muscle Strength", "Back Pain Relief"
                  ].map((g) => (
                    <div
                      key={g}
                      onClick={() => setFormGoal(g as any)}
                      className={`cursor-pointer p-3.5 rounded-xl border text-center transition-all flex flex-col justify-center items-center shadow-xs ${
                        formGoal === g 
                          ? "bg-purple-100/90 border-[#7c3aed] text-[#5c248d] font-extrabold" 
                          : "bg-purple-50/20 border-purple-150 hover:bg-purple-100/50 text-slate-600 font-bold"
                      }`}
                    >
                      <strong className="text-xs uppercase tracking-tight block">{g}</strong>
                    </div>
                  ))}
                </div>
              </div>

              {/* Flexibility Level & Experience */}
              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                
                <div className="space-y-2">
                  <label className="text-xs font-mono font-bold uppercase tracking-wider text-[#3b1259] block">
                    Current Flexibility Level
                  </label>
                  <div className="flex gap-2">
                    {["Beginner", "Moderate", "Advanced"].map((level) => (
                      <button
                        type="button"
                        key={level}
                        onClick={() => setFormFlexibility(level as any)}
                        className={`flex-grow py-2.5 rounded-xl border text-center font-bold text-xs cursor-pointer transition-all ${
                          formFlexibility === level 
                            ? "bg-purple-100/90 border-[#7c3aed] text-[#5c248d] font-black shadow-xs" 
                            : "bg-purple-50/20 border-purple-150 hover:bg-[#FAF8FE] text-slate-600"
                        }`}
                      >
                        {level}
                      </button>
                    ))}
                  </div>
                </div>

                <div className="space-y-2">
                  <label className="text-xs font-mono font-bold uppercase tracking-wider text-[#3b1259] block">
                    Yoga Experience
                  </label>
                  <div className="flex gap-2">
                    {["Beginner", "Intermediate", "Advanced"].map((exp) => (
                      <button
                        type="button"
                        key={exp}
                        onClick={() => setFormExperience(exp as any)}
                        className={`flex-grow py-2.5 rounded-xl border text-center font-bold text-xs cursor-pointer transition-all ${
                          formExperience === exp 
                            ? "bg-purple-100/90 border-[#7c3aed] text-[#5c248d] font-black shadow-xs" 
                            : "bg-purple-50/20 border-purple-150 hover:bg-[#FAF8FE] text-slate-600"
                        }`}
                      >
                        {exp}
                      </button>
                    ))}
                  </div>
                </div>

              </div>

              <div className="pt-4">
                <button
                  type="submit"
                  disabled={isAssessing}
                  className="w-full py-4 bg-gradient-to-r from-purple-600 via-indigo-600 to-pink-500 text-white font-extrabold rounded-xl shadow-lg shadow-purple-200/40 cursor-pointer uppercase transition-all tracking-wider text-sm flex items-center justify-center gap-2"
                  id="calculate-yogic-formula-submit"
                >
                  {isAssessing ? (
                    <>
                      <span className="w-4 h-4 rounded-full border-2 border-white/30 border-t-white animate-spin" />
                      Acharya is Compiling Divine Blueprint...
                    </>
                  ) : (
                    <>
                      <Sparkles className="w-5 h-5 text-yellow-300 animate-pulse" />
                      Calculate Yogic Path
                    </>
                  )}
                </button>
              </div>

            </form>
          </motion.section>
        )}

        {/* EXPLORE POSES COMPLETE ENCYCLOPEDIA VIEW */}
        {activeTab === "poses" && (
          <motion.section 
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            className="space-y-8"
          >
            <div className="text-center max-w-xl mx-auto space-y-2">
              <span className="text-pink-600 font-mono text-xs uppercase tracking-widest font-bold">Divine Dictionary</span>
              <h2 className="text-3xl font-black text-[#210833] uppercase tracking-tight">Interactive Pose Encyclopedia</h2>
              <p className="text-xs text-slate-550 font-medium">
                Praise ancient alignment with modern physical rehabilitation. Select standard poses from the matrix below to open customized guidance blocks in real-time.
              </p>
            </div>

            {/* Complete grid of presets */}
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
              {Object.values(PRESET_POSES).map((pose) => (
                <div
                  key={pose.id}
                  onClick={() => setSelectedPose(pose)}
                  className="bg-white border border-purple-100 p-4 rounded-2xl cursor-pointer hover:border-purple-300 hover:bg-purple-50/20 transition-all flex items-center gap-4 shadow-xs"
                >
                  <PoseVisualizer poseId={pose.id} isBreathing={false} size={80} className="bg-purple-100/30 p-1 shrink-0 rounded-xl" />
                  
                  <div className="min-w-0">
                    <span className="text-[9px] font-mono font-bold text-pink-600 bg-pink-50 border border-pink-100 px-1.5 py-0.5 rounded leading-none">
                      {pose.sanskritName}
                    </span>
                    <h3 className="text-base font-extrabold text-[#210833] truncate mt-1 leading-snug">
                      {pose.name}
                    </h3>
                    <div className="flex items-center gap-2 mt-1">
                      <span className="text-[10px] text-slate-500 font-bold">Est. {pose.calories} kcal</span>
                      <span className="text-[10px] text-[#5c248d] font-bold">{pose.duration}</span>
                    </div>
                  </div>
                </div>
              ))}
            </div>

            {/* Categorized Filter Program Card layout */}
            <div className="bg-[#FAF8FE] rounded-3xl border border-purple-100 p-6 md:p-8 shadow-sm">
              <h3 className="text-lg font-extrabold text-[#210833] uppercase tracking-wider mb-4 text-center md:text-left">
                Suggested Curations Categories
              </h3>
              
              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                {[
                  {
                    title: "Meditation & Sahasrara Union",
                    desc: "Restorative, gentle breathing alignments centered on quietening cortical noise and dropping levels of adrenaline.",
                    poses: "Sukhasana, Child's Pose, Butterfly Pose",
                    duration: "10 Minutes"
                  },
                  {
                    title: "Metabolic Fire Power",
                    desc: "Higher physical requirements configured to cultivate metabolic Tapas, heat, and joint strength.",
                    poses: "Warrior II, Cobra Pose, Downward-Facing Dog",
                    duration: "15 Minutes"
                  },
                  {
                    title: "Golden Joint Restorative",
                    desc: "Specially formulated for seniors or office desks to safely rotate stiff ligaments and alleviate neck compressed strain.",
                    poses: "Cat-Cow Pose, Mountain Pose, Gentle Chair Stretch",
                    duration: "12 Minutes"
                  }
                ].map((program, idx) => (
                  <div key={idx} className="bg-white border border-purple-100 p-5 rounded-2xl flex flex-col justify-between shadow-sm">
                    <div>
                      <span className="text-[9px] font-mono text-pink-600 font-extrabold uppercase bg-pink-50 border border-pink-100 p-1 rounded">Curated Group</span>
                      <h4 className="text-base font-black text-[#210833] tracking-wide mt-2">{program.title}</h4>
                      <p className="text-xs text-slate-600 leading-relaxed font-sans font-semibold mt-2">{program.desc}</p>
                    </div>

                    <div className="border-t border-purple-100 pt-3 mt-4 flex items-center justify-between text-[11px] text-slate-500">
                      <span>Includes: <strong className="text-slate-705 font-bold">{program.poses}</strong></span>
                      <span className="text-purple-752 font-black shrink-0">{program.duration}</span>
                    </div>
                  </div>
                ))}
              </div>
            </div>

          </motion.section>
        )}

        {/* PROGRESS TRACKER DASHBOARD VIEW */}
        {activeTab === "tracker" && (
          <motion.section 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            className="space-y-8"
          >
            <div className="text-center max-w-xl mx-auto space-y-2">
              <span className="text-pink-600 font-mono text-xs uppercase tracking-widest font-bold">Biomechanics Analytics</span>
              <h2 className="text-3xl font-black text-[#210833] uppercase tracking-tight">Your Sanctuary Dashboard</h2>
              <p className="text-xs text-slate-550 font-medium">
                Witness your physical expansion and breathing streak counter records beautifully synced inside client state database.
              </p>
            </div>

            {/* Main Stats Widgets Grid */}
            <div className="grid grid-cols-1 md:grid-cols-4 gap-6">
              
              {/* Daily Streak Indicator */}
              <div className="bg-white border border-purple-100 p-6 rounded-2xl flex flex-col justify-between text-center md:text-left relative shadow-sm">
                <div className="absolute top-3 right-3 text-pink-600 opacity-15">
                  <Flame className="w-12 h-12 fill-current" />
                </div>
                <div>
                  <span className="text-[10px] font-mono uppercase tracking-widest text-[#be185d] font-bold">Active Meditation Days</span>
                  <strong className="block text-4xl font-extrabold text-[#210833] mt-2 font-serif">{streakCount} Days</strong>
                  <p className="text-xs text-slate-600 leading-relaxed font-semibold mt-1">
                    Keep your practice active daily to keep your internal fire (Tapas) boiling of toxins.
                  </p>
                </div>
                <div className="pt-4 border-t border-purple-100 mt-4 text-[10px] font-mono text-purple-700 font-bold">
                  Streak record updated: May 28, 2026
                </div>
              </div>

              {/* Estimated Total Calories */}
              <div className="bg-white border border-purple-100 p-6 rounded-2xl flex flex-col justify-between text-center md:text-left relative shadow-sm">
                <div className="absolute top-3 right-3 text-orange-600 opacity-15">
                  <Flame className="w-12 h-12 fill-current" />
                </div>
                <div>
                  <span className="text-[10px] font-mono uppercase tracking-widest text-slate-500 font-bold">Total Curated Calories</span>
                  <strong className="block text-4xl font-extrabold text-[#210833] mt-2 font-serif">
                    {weeklyHistory.reduce((sum: number, cur: any) => sum + cur.calories, 0)} kcal
                  </strong>
                  <p className="text-xs text-slate-600 leading-relaxed font-semibold mt-1">
                    Based on metabolic weights of current postures list configured in weekly timelines.
                  </p>
                </div>
                <div className="pt-4 border-t border-purple-100 mt-4 text-[10px] font-mono text-purple-700 font-bold">
                  Est. weights tuned to {profile.weight}kg envelope.
                </div>
              </div>

              {/* Total Practice Duration */}
              <div className="bg-white border border-purple-100 p-6 rounded-2xl flex flex-col justify-between text-center md:text-left relative shadow-sm">
                <div className="absolute top-3 right-3 text-emerald-600 opacity-15">
                  <Timer className="w-12 h-12" />
                </div>
                <div>
                  <span className="text-[10px] font-mono uppercase tracking-widest text-slate-500 font-bold">Total Duration Minutes</span>
                  <strong className="block text-4xl font-extrabold text-[#210833] mt-2 font-serif">
                    {weeklyHistory.reduce((sum: number, cur: any) => sum + cur.duration, 0)} mins
                  </strong>
                  <p className="text-xs text-slate-600 leading-relaxed font-semibold mt-1">
                    Total respiratory and muscular flow minutes kept active inside sessions.
                  </p>
                </div>
                <div className="pt-4 border-t border-purple-100 mt-4 text-[10px] font-mono text-purple-700 font-bold">
                  Vinyasa speeds: Slow / diaphragmatic control.
                </div>
              </div>

              {/* Simulated Flexibility growth card */}
              <div className="bg-white border border-purple-100 p-6 rounded-2xl flex flex-col justify-between text-center md:text-left relative shadow-sm">
                <div className="absolute top-3 right-3 text-indigo-600 opacity-15">
                  <Award className="w-12 h-12" />
                </div>
                <div>
                  <span className="text-[10px] font-mono uppercase tracking-widest text-slate-500 font-bold">Yogic Balance Scale</span>
                  <strong className="block text-4xl font-extrabold text-[#210833] mt-2 font-serif">86% Grade</strong>
                  <p className="text-xs text-slate-600 leading-relaxed font-semibold mt-1">
                    Current alignment feedback index calculated based on {profile.flexibility} flexibility and {profile.experience} level.
                  </p>
                </div>
                <div className="pt-4 border-t border-purple-100 mt-4 text-[10px] font-mono text-purple-700 font-bold">
                  Dynamic safety bounds: Fully enabled.
                </div>
              </div>

            </div>

            {/* D3-style beautiful fully hand-crafted clean SVG Analytics bar chart */}
            <div className="bg-white p-6 rounded-3xl border border-purple-100 shadow-sm">
              <h3 className="text-sm font-bold font-mono uppercase tracking-widest text-[#210833] mb-6">
                Historic Volume representation (postures parsed)
              </h3>
              
              <div className="h-64 flex items-end justify-between gap-6 px-4 pt-10 border-b border-purple-100 relative">
                
                {/* Horizontal reference lines */}
                <div className="absolute left-0 right-0 top-[25%] border-t border-purple-100 border-dashed pointer-events-none opacity-50" />
                <div className="absolute left-0 right-0 top-[50%] border-t border-purple-100 border-dashed pointer-events-none opacity-50" />
                <div className="absolute left-0 right-0 top-[75%] border-t border-purple-100 border-dashed pointer-events-none opacity-50" />

                {weeklyHistory.map((day: any, i: number) => {
                  const maxDuration = Math.max(...weeklyHistory.map((d: any) => d.duration), 30);
                  const heightPercentage = Math.round((day.duration / maxDuration) * 90);

                  return (
                    <div key={i} className="flex-1 flex flex-col items-center group relative z-10">
                      
                      {/* Tooltip */}
                      <div className="absolute bottom-full mb-2 bg-[#210833] text-white rounded-lg px-2.5 py-1 text-[10px] font-mono opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap shadow-xl">
                        Duration: {day.duration} min | Ref cal: {day.calories} kcal
                      </div>

                      {/* Bar columns */}
                      <div 
                        style={{ height: `${heightPercentage}%` }}
                        className="w-full max-w-[40px] rounded-t-lg bg-gradient-to-t from-indigo-600 via-[#7c3aed] to-pink-500 shadow-sm group-hover:opacity-90 transition-all cursor-pointer relative"
                      >
                        <div className="absolute inset-x-0 top-0 h-1 bg-white/40 rounded-t-lg pointer-events-none" />
                      </div>

                      {/* Label under Column */}
                      <span className="text-xs text-slate-400 font-mono mt-2 leading-none uppercase select-none">
                        {day.date}
                      </span>
                    </div>
                  );
                })}

              </div>
              
              {/* Chart Legend info */}
              <div className="flex items-center justify-center gap-6 mt-6 text-xs text-slate-400 font-medium">
                <div className="flex items-center gap-1.5">
                  <span className="w-3 h-3 bg-gradient-to-tr from-purple-600 to-pink-500 rounded-full" />
                  <span>Calculated Routine Practice minutes record</span>
                </div>
              </div>

            </div>

            {/* INTERACTIVE TAPAS DAILY STREAK HEATING GAME ARENA */}
            <div className="bg-gradient-to-b from-[#FAF5FF] to-white p-6 md:p-8 rounded-3xl border border-purple-100 shadow-md space-y-8" id="streak-game-board">
              <div className="flex flex-col sm:flex-row items-center justify-between gap-4 border-b border-purple-100 pb-5">
                <div className="flex items-center gap-3">
                  <div className="bg-pink-100 p-3 rounded-2xl text-pink-600 relative">
                    <Flame className="w-6 h-6 fill-pink-600 animate-pulse" />
                    <span className="absolute -top-1 -right-1 flex h-2 w-2">
                      <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-pink-400 opacity-75"></span>
                      <span className="relative inline-flex rounded-full h-2 w-2 bg-pink-500"></span>
                    </span>
                  </div>
                  <div>
                    <h3 className="text-lg font-black text-[#210833] uppercase tracking-wide flex items-center gap-1.5">
                      Tapas Fire-Streak Arena <span className="text-xs bg-purple-150 text-purple-700 font-mono font-black py-0.5 px-2 rounded-full uppercase">Minigame</span>
                    </h3>
                    <p className="text-xs text-slate-600 leading-relaxed font-semibold">
                      Click any day below to register your mindfulness session, fan the spiritual ash, and earn sacred aura badges!
                    </p>
                  </div>
                </div>
                <div className="flex items-center gap-2">
                  <span className="text-[10px] font-mono uppercase bg-amber-100 text-amber-800 font-extrabold px-3 py-1.5 rounded-xl border border-amber-200">
                    🔥 All-Time High: {allTimeHighStreak} Days
                  </span>
                  <span className="text-[10px] font-mono uppercase bg-purple-100 text-purple-800 font-extrabold px-3 py-1.5 rounded-xl border border-purple-200">
                    💨 Fanned: {fannedFlameCount} times
                  </span>
                </div>
              </div>

              {/* Main Game Grid: Left/Right columns */}
              <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-stretch">
                
                {/* COLUMN 1 (SPAN 7): INTERACTIVE WEEKDAYS CHECKLIST */}
                <div className="lg:col-span-7 space-y-6 flex flex-col justify-between">
                  <div>
                    <span className="text-[10px] font-mono uppercase text-slate-500 tracking-wider font-extrabold block mb-3">
                      📅 Weekly Tapas Check-In Board (Click to Toggle Days)
                    </span>
                    
                    <div className="grid grid-cols-7 gap-2">
                      {[
                        { key: "Mon", label: "M", title: "Monday" },
                        { key: "Tue", label: "T", title: "Tuesday" },
                        { key: "Wed", label: "W", title: "Wednesday" },
                        { key: "Thu", label: "T", title: "Thursday" },
                        { key: "Fri", label: "F", title: "Friday" },
                        { key: "Sat", label: "S", title: "Saturday" },
                        { key: "Sun", label: "S", title: "Sunday" }
                      ].map((dayItem) => {
                        const isDone = completedDays.includes(dayItem.key);
                        return (
                          <motion.button
                            key={dayItem.key}
                            whileHover={{ scale: 1.08 }}
                            whileTap={{ scale: 0.95 }}
                            onClick={() => {
                              let updated;
                              if (completedDays.includes(dayItem.key)) {
                                updated = completedDays.filter(d => d !== dayItem.key);
                                playSillySlide();
                              } else {
                                updated = [...completedDays, dayItem.key];
                                playSuccessFanfare();
                              }
                              setCompletedDays(updated);
                              localStorage.setItem("yogasan_streak_days", JSON.stringify(updated));

                              // Sync streak count logic based on active check-in count
                              const newSt = updated.length;
                              setStreakCount(newSt);
                              localStorage.setItem("yogasan_streak", newSt.toString());
                              if (newSt > allTimeHighStreak) {
                                setAllTimeHighStreak(newSt);
                                localStorage.setItem("yogasan_high_streak", newSt.toString());
                              }
                            }}
                            className={`flex flex-col items-center gap-2 p-3 rounded-2xl cursor-pointer border-2 transition-all select-none ${
                              isDone
                                ? "bg-gradient-to-br from-pink-500 to-purple-600 text-white border-pink-500 shadow-md shadow-pink-550/10"
                                : "bg-white text-slate-400 border-slate-100 hover:border-purple-200"
                            }`}
                            title={`Toggle check-in completion for ${dayItem.title}`}
                          >
                            <span className="text-[10px] font-mono font-black uppercase text-center w-full block">
                              {dayItem.key}
                            </span>
                            <div className={`w-8 h-8 rounded-full flex items-center justify-center font-bold text-xs ${
                              isDone ? "bg-white/20" : "bg-slate-50 text-slate-600"
                            }`}>
                              {isDone ? (
                                <Flame className="w-4 h-4 fill-white text-white" />
                              ) : (
                                dayItem.label
                              )}
                            </div>
                            <span className="text-[8px] font-mono opacity-80 uppercase font-bold tracking-tighter">
                              {isDone ? "BURN" : "REST"}
                            </span>
                          </motion.button>
                        );
                      })}
                    </div>
                  </div>

                  <div className="bg-amber-50/50 rounded-2xl border border-amber-150 p-4 space-y-2 mt-4">
                    <div className="flex items-center gap-2 text-xs font-black text-[#5c248d] uppercase">
                      <Sparkles className="w-4 h-4 text-amber-500" /> Tapas Fire Spiritual Level
                    </div>
                    <p className="text-xs text-slate-700 font-medium">
                      Your current active heat level is <strong className="text-pink-600 underline">{streakCount} Days</strong>. Keep completing postures to nourish your glowing energy centers!
                    </p>
                    <div className="w-full bg-slate-200/50 h-2.5 rounded-full overflow-hidden mt-2 relative">
                      <div 
                        style={{ width: `${Math.min((streakCount / 7) * 100, 100)}%` }}
                        className="h-full bg-gradient-to-r from-pink-500 via-purple-500 to-indigo-600 transition-all duration-500"
                      />
                    </div>
                    <div className="flex justify-between items-center text-[9px] font-mono text-slate-400 font-extrabold uppercase mt-1">
                      <span>0 Days (Spout)</span>
                      <span>{streakCount} Active</span>
                      <span>7 Days (Supernova)</span>
                    </div>
                  </div>
                </div>

                {/* COLUMN 2 (SPAN 5): HIGHLY INTERACTIVE FIRE PIT */}
                <div className="lg:col-span-5 bg-white border border-purple-100 rounded-3xl p-5 flex flex-col items-center justify-between text-center relative overflow-hidden shadow-xs">
                  
                  {/* Fire smoke bg particle graphics decoration */}
                  <div className="absolute inset-0 bg-gradient-to-b from-transparent to-pink-500/5 pointer-events-none" />

                  <span className="text-[9px] font-mono uppercase tracking-widest text-slate-500 font-bold relative z-10">
                    🔥 HEAR THE SACRED TAPAS FIRE
                  </span>

                  {/* SVG Fire-Pit Canvas - Responding dramatically to actual streak count */}
                  <div className="w-32 h-32 my-2 flex items-center justify-center relative cursor-pointer group" title="Tap inside to stoke the flame!">
                    <motion.svg
                      viewBox="0 0 100 100" 
                      onClick={() => {
                        const nextF = fannedFlameCount + 1;
                        setFannedFlameCount(nextF);
                        localStorage.setItem("yogasan_fanned_flame", nextF.toString());
                        playBubblePop();
                        playSillySlide();

                        const fanners = [
                          "Whoosh! You stoke the spiritual wood. 🪵 Feel the metabolic heat rise!",
                          "Fanning the Tapas melts mental friction blocks!",
                          "Tapas is boiling away all micro-toxins! Breathe deeply!",
                          "Beautiful yellow sparks fly into the astral dimension! ✨"
                        ];
                        setAvocadoDialogue(fanners[Math.floor(Math.random() * fanners.length)]);
                        setAvocadoMood("CRAZY");
                      }}
                      className="w-full h-full"
                      animate={{ scale: [1, 1.05, 1], rotate: [0, 1, -1, 0] }}
                      transition={{ repeat: Infinity, duration: 4, ease: "easeInOut" }}
                    >
                      {/* Grey firewood base */}
                      <rect x="25" y="80" width="50" height="8" rx="4" fill="#6B7280" />
                      <line x1="30" y1="84" x2="70" y2="84" stroke="#4B5563" strokeWidth="2" strokeLinecap="round" />
                      
                      {/* Fire charcoal glows */}
                      <circle cx="50" cy="80" r="15" fill="#EF4444" opacity="0.4" />
                      <circle cx="50" cy="80" r="7" fill="#F59E0B" opacity="0.6" />

                      {/* Giant Fire Flames - scaled up dynamically reflecting streak count! */}
                      {/* Height and width values increase based on streak count */}
                      <g style={{ transform: `scale(${0.6 + (streakCount * 0.1)})`, transformOrigin: "50px 80px" }} className="transition-all duration-300">
                        {/* Red Main back flame overlay */}
                        <path
                          d="M 50 20 Q 25 65, 30 80 Q 50 82, 70 80 Q 75 65, 50 20 Z"
                          fill="#EF4444"
                          opacity="0.85"
                        />
                        {/* Orange wave inner flame */}
                        <path
                          d="M 50 35 Q 32 68, 36 80 Q 50 82, 64 80 Q 68 68, 50 35 Z"
                          fill="#F97316"
                          opacity="0.9"
                        />
                        {/* Golden core light source */}
                        <path
                          d="M 50 50 Q 38 72, 40 80 Q 50 81, 60 80 Q 62 72, 50 50 Z"
                          fill="#FBBF24"
                        />
                        {/* Pure White nucleus */}
                        <ellipse cx="50" cy="74" rx="5" ry="6" fill="#FFFFFF" opacity="0.9" />
                      </g>
                    </motion.svg>
                    
                    {/* Animated sparks floating around fire */}
                    <div className="absolute top-2 w-full flex justify-between px-6 pointer-events-none">
                      <span className="text-yellow-400 text-xs animate-bounce select-none">✨</span>
                      <span className="text-orange-400 text-sm animate-pulse select-none" style={{ animationDelay: "1s" }}>🔥</span>
                      <span className="text-pink-400 text-xs animate-bounce select-none" style={{ animationDelay: "1.5s" }}>✨</span>
                    </div>
                  </div>

                  <div className="space-y-3 w-full">
                    <button
                      onClick={() => {
                        const nextF = fannedFlameCount + 1;
                        setFannedFlameCount(nextF);
                        localStorage.setItem("yogasan_fanned_flame", nextF.toString());
                        playBubblePop();
                        playSillySlide();

                        const list = [
                          "Whoosh! Fresh oxygen fanned! 🔥 Posture feels sturdy!",
                          "Beautiful cosmic sparks rising into the ceiling of your heart!",
                          "Every click represents absolute discipline and love!",
                          "Spiritual Tapas expands metabolics!"
                        ];
                        setAvocadoDialogue(list[Math.floor(Math.random() * list.length)]);
                        setAvocadoMood("HAPPY");
                      }}
                      className="w-full bg-[#7C3AED] hover:bg-[#6D28D9] text-white py-2 px-4 rounded-2xl text-xs font-black uppercase tracking-wider transition-colors shadow-sm cursor-pointer border-none"
                    >
                      🔥 Fan Tapas Fire Beat!
                    </button>
                    <p className="text-[10px] text-slate-500 font-bold leading-normal">
                      Click the stoker button to fan physical heat waves, trigger synthesizers, and stimulate biochemical clarity!
                    </p>
                  </div>
                </div>

              </div>

              {/* ACHIEVEMENTS BLOCK: BADGES UNLOCKED BASED ON STREAKS */}
              <div className="pt-6 border-t border-purple-100">
                <span className="text-[10px] font-mono tracking-widest text-slate-500 uppercase font-extrabold block mb-4">
                  🏆 Tapas Aura Badges & Meditative Accomplishments
                </span>
                
                <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
                  
                  {/* BADGE 1: Pranayama Prodigy (>=3 days checked) */}
                  <div className={`p-4 rounded-2xl border flex items-center gap-3 transition-opacity ${
                    completedDays.length >= 3 
                      ? "bg-violet-50 text-violet-900 border-violet-100" 
                      : "bg-slate-50/50 text-slate-400 border-slate-100 opacity-60"
                  }`}>
                    <div className={`p-2 rounded-xl shrink-0 ${
                      completedDays.length >= 3 ? "bg-violet-200 text-violet-700" : "bg-slate-100 text-slate-400"
                    }`}>
                      <Award className="w-5 h-5" />
                    </div>
                    <div>
                      <h4 className="text-[11px] font-mono font-black uppercase">
                        Pranayama Prodigy
                      </h4>
                      <p className="text-[9px] font-semibold mt-0.5 leading-tight">
                        Check off at least 3 days this week. {completedDays.length >= 3 ? "⭐ ACHIEVED" : `${3 - completedDays.length} more needed`}
                      </p>
                    </div>
                  </div>

                  {/* BADGE 2: Tapas Trailblazer (Streak >=5 Or fanned count >= 5) */}
                  <div className={`p-4 rounded-2xl border flex items-center gap-3 transition-opacity ${
                    streakCount >= 5 || fannedFlameCount >= 5
                      ? "bg-amber-50 text-amber-900 border-amber-100" 
                      : "bg-slate-50/50 text-slate-400 border-slate-100 opacity-60"
                  }`}>
                    <div className={`p-2 rounded-xl shrink-0 ${
                      streakCount >= 5 || fannedFlameCount >= 5 ? "bg-amber-200 text-amber-700 font-bold" : "bg-slate-100 text-slate-400"
                    }`}>
                      <Flame className="w-5 h-5 fill-current" />
                    </div>
                    <div>
                      <h4 className="text-[11px] font-mono font-black uppercase">
                        Tapas Trailblazer
                      </h4>
                      <p className="text-[9px] font-semibold mt-0.5 leading-tight">
                        Stoke fanned flames 5+ times or reach 5 streak level. { (streakCount >= 5 || fannedFlameCount >= 5) ? "🌟 UNLOCKED" : `${5 - fannedFlameCount} fans needed`}
                      </p>
                    </div>
                  </div>

                  {/* BADGE 3: Cosmic Supernova (Streak >=7) */}
                  <div className={`p-4 rounded-2xl border flex items-center gap-3 transition-opacity ${
                    streakCount >= 7 
                      ? "bg-rose-50 text-rose-905 border-rose-100 animate-pulse" 
                      : "bg-slate-50/50 text-slate-350 border-slate-100 opacity-60"
                  }`}>
                    <div className={`p-2 rounded-xl shrink-0 ${
                      streakCount >= 7 ? "bg-rose-200 text-rose-600" : "bg-slate-100 text-slate-400"
                    }`}>
                      <Sparkles className="w-5 h-5" />
                    </div>
                    <div>
                      <h4 className="text-[11px] font-mono font-black uppercase">
                        Cosmic Supernova
                      </h4>
                      <p className="text-[9px] font-semibold mt-0.5 leading-tight">
                        Reach a full 7-day master streak cycle. {streakCount >= 7 ? "👑 ENLIGHTENED YOGI!" : `${7 - streakCount} days left`}
                      </p>
                    </div>
                  </div>

                </div>
              </div>

            </div>

          </motion.section>
        )}
        
        {/* CUTE HEALTHY RECIPES & YOGA JOKES VIEW */}
        {activeTab === "cute" && (
          <motion.section 
            initial={{ opacity: 0, y: 15 }}
            animate={{ opacity: 1, y: 0 }}
            className="space-y-10"
          >
            {/* Header Banter */}
            <div className="text-center max-w-xl mx-auto space-y-3">
              <span className="text-pink-600 font-mono text-xs uppercase tracking-widest font-extrabold bg-pink-100 py-1 px-4.5 rounded-full border border-pink-200 inline-block animate-bounce shadow-sm">
                🥑 ✨ Cozy Hub for Healthy Smiles ✨ 🥑
              </span>
              <h2 className="text-3xl font-black text-[#210833] uppercase tracking-tight">Cozy Yogi Kitchen & Daily Giggles</h2>
              <p className="text-xs text-slate-600 font-bold leading-relaxed">
                Wholesome metabolic food secrets and pure tickling yoga puns! Made for real, everyday folks seeking wellness with a beautiful giant smile.
              </p>
            </div>

            {/* Main Interactive Grid */}
            <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-start">
              
              {/* Left Column (SPAN 7): Healthy Yogi Cookbook */}
              <div className="lg:col-span-7 bg-white rounded-3xl border-4 border-dashed border-pink-200 p-6 md:p-8 shadow-sm space-y-6 relative">
                {/* Cute corner absolute graphics */}
                <div className="absolute top-3 right-3 text-3xl select-none animate-pulse">👩‍🍳</div>
                
                <div>
                  <h3 className="text-2xl font-black text-[#210833] uppercase tracking-tight flex items-center gap-2">
                    🥑 Yogi Cookbook
                  </h3>
                  <p className="text-xs text-slate-500 font-bold">
                    Easy, colorful Ayurvedic-inspired recipes to energize your muscle cells and soothe digestion.
                  </p>
                </div>

                {/* Recipe selection bubbles */}
                <div className="grid grid-cols-2 sm:grid-cols-4 gap-2">
                  {HEALTHY_RECIPES.map((rec, rIdx) => (
                    <button
                      key={rIdx}
                      type="button"
                      onClick={() => setSelectedRecipeIdx(rIdx)}
                      className={`text-[11px] p-3 rounded-2xl font-extrabold border-2 transition-all text-center flex flex-col justify-center items-center cursor-pointer gap-1 ${
                        selectedRecipeIdx === rIdx
                          ? "bg-pink-100/90 border-pink-400 text-pink-700 shadow-sm font-black scale-102"
                          : "bg-purple-50/20 border-purple-100 hover:bg-purple-100/40 text-slate-600"
                      }`}
                    >
                      <span className="text-xl">{rec.name.split(" ").slice(-1)[0]}</span>
                      <span className="truncate max-w-full leading-tight">{rec.name.split(" ").slice(0, -1).join(" ")}</span>
                    </button>
                  ))}
                </div>

                {/* Selected Recipe Card Display */}
                <motion.div
                  key={selectedRecipeIdx}
                  initial={{ opacity: 0, x: -10 }}
                  animate={{ opacity: 1, x: 0 }}
                  className="bg-purple-50/40 rounded-2xl p-5 border border-purple-100 space-y-5"
                >
                  {/* Title Bar */}
                  <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-purple-100/85 pb-4">
                    <div>
                      <h4 className="text-lg font-black text-slate-900">{HEALTHY_RECIPES[selectedRecipeIdx].name}</h4>
                      <p className="text-xs text-pink-600 font-bold italic">{HEALTHY_RECIPES[selectedRecipeIdx].slogan}</p>
                    </div>
                    <div className="flex items-center gap-2 text-[10px] font-mono font-bold uppercase shrink-0">
                      <span className="bg-amber-100 text-amber-800 border border-amber-250 px-2.5 py-0.5 rounded-full">
                        ⏱️ {HEALTHY_RECIPES[selectedRecipeIdx].prepTime}
                      </span>
                      <span className="bg-emerald-100 text-emerald-800 border border-emerald-250 px-2.5 py-0.5 rounded-full">
                        ⭐ {HEALTHY_RECIPES[selectedRecipeIdx].difficulty}
                      </span>
                    </div>
                  </div>

                  {/* Nutrition attributes */}
                  <div className="grid grid-cols-4 gap-2 bg-white rounded-xl p-3 text-center border border-purple-50">
                    {Object.entries(HEALTHY_RECIPES[selectedRecipeIdx].nutrition).map(([key, value]) => (
                      <div key={key} className="min-w-0">
                        <span className="block text-[8px] uppercase font-mono tracking-wider text-slate-500 font-bold">{key}</span>
                        <strong className="text-xs text-purple-900 font-extrabold truncate block">{value}</strong>
                      </div>
                    ))}
                  </div>

                  {/* Ingredients Checklist */}
                  <div className="space-y-2">
                    <h5 className="text-xs font-mono font-black uppercase tracking-wider text-slate-700 flex items-center gap-1">
                      🥗 Ingredients list
                    </h5>
                    <ul className="grid grid-cols-1 sm:grid-cols-2 gap-2">
                      {HEALTHY_RECIPES[selectedRecipeIdx].ingredients.map((ing, iIdx) => (
                        <li key={iIdx} className="flex items-center gap-2 bg-white p-2 rounded-lg border border-purple-50 text-xs text-slate-700 font-bold shadow-xs">
                          <span className="text-emerald-500 font-extrabold">✓</span>
                          <span>{ing}</span>
                        </li>
                      ))}
                    </ul>
                  </div>

                  {/* Steps with cute visual layout */}
                  <div className="space-y-2">
                    <h5 className="text-xs font-mono font-black uppercase tracking-wider text-slate-700 flex items-center gap-1">
                      👩‍🍳 Cozy Steps
                    </h5>
                    <ol className="space-y-2.5">
                      {HEALTHY_RECIPES[selectedRecipeIdx].steps.map((step, sIdx) => (
                        <li key={sIdx} className="flex gap-2.5 bg-white p-3 rounded-xl border border-purple-100 shadow-xs">
                          <span className="w-5 h-5 rounded-full bg-pink-100 text-pink-700 font-mono text-[10px] font-black flex items-center justify-center shrink-0">
                            {sIdx + 1}
                          </span>
                          <p className="text-xs text-slate-700 font-semibold leading-relaxed">{step}</p>
                        </li>
                      ))}
                    </ol>
                  </div>

                  {/* Chef's tip panel */}
                  <div className="bg-emerald-50 rounded-xl p-3 border border-emerald-100 flex items-start gap-2.5">
                    <span className="text-lg">💡</span>
                    <p className="text-xs text-slate-700 font-bold leading-snug">
                      <strong>Cuddly Tip:</strong> {HEALTHY_RECIPES[selectedRecipeIdx].chefTip}
                    </p>
                  </div>

                </motion.div>
              </div>

              {/* Right Column (SPAN 5): Cute Daily Giggles Card */}
              <div className="lg:col-span-5 flex flex-col gap-6">

                {/* Animated Cute Avocado Joke Teller */}
                <div className="bg-[#FFFDF6] rounded-3xl border-4 border-dashed border-amber-200 p-6 md:p-8 shadow-sm flex flex-col items-center relative overflow-hidden">
                  {/* Decorative background sun rays */}
                  <div className="absolute inset-x-0 top-0 h-1/2 bg-gradient-to-b from-amber-100/30 to-transparent pointer-events-none" />
                  
                  <span className="inline-flex items-center gap-1.5 text-[10px] font-mono uppercase bg-amber-100 text-amber-800 border border-amber-200 px-3 py-1 rounded-full font-bold relative z-10 animate-pulse">
                    😂🧘‍♂️ INTERACTIVE AVOCADO COMPANION PET
                  </span>

                  {/* Reactive Companion Speech bubble */}
                  <div className="w-full bg-pink-50 border-2 border-pink-200 rounded-2xl p-4 mt-4 text-center text-xs font-bold text-pink-800 relative shadow-xs">
                    <div className="absolute bottom-[-8px] left-1/2 -translate-x-1/2 w-3.5 h-3.5 bg-pink-50 border-b-2 border-r-2 border-pink-200 rotate-45" />
                    <p className="relative z-10 leading-relaxed font-sans">
                      💬 "{avocadoDialogue}"
                    </p>
                    <div className="text-[9px] text-[#5c248d] font-mono mt-1 font-black uppercase">
                      Current Mood: {avocadoMood === "ZEN" ? "🧘 Zen Master" : avocadoMood === "CRAZY" ? "🤪 Hyper / Wobbly" : avocadoMood === "SLEEPY" ? "💤 Fluffy Sleepy" : "🍉 Deliciously Happy"}
                    </div>
                  </div>

                  {/* Cute SVG Character Illustration - Blushing Avocado Doing Yoga! */}
                  <div 
                    onClick={() => {
                      playBubblePop();
                      const tickleLines = [
                        "Ooh, that tickles my Haas shell! 🥑 Keep stretching!",
                        "Namaste! Did you know laughing is a great isometric core exercise?",
                        "Your touch is full of metabolic light speed!",
                        "Breathing with you is my favourite posture!"
                      ];
                      const randLine = tickleLines[Math.floor(Math.random() * tickleLines.length)];
                      setAvocadoDialogue(randLine);
                    }}
                    className="w-48 h-48 my-2 flex items-center justify-center relative cursor-pointer group" 
                    title="Click to Tickle the Avocado!"
                  >
                    <svg viewBox="0 0 100 100" className={`w-full h-full drop-shadow-[0_4px_8px_rgba(202,138,4,0.15)] transition-transform duration-300 ${
                      avocadoMood === "CRAZY" ? "animate-bounce" : avocadoMood === "SLEEPY" ? "opacity-90" : "group-hover:scale-105"
                    }`}>
                      {/* Body shadow */}
                      <ellipse cx="50" cy="88" rx="22" ry="5" fill="#E2E8F0" opacity="0.6" />
                      
                      {/* Avocado body (pear shape outer line) */}
                      <path d="M 50 15 C 38 15, 34 40, 30 60 C 26 80, 40 90, 50 90 C 60 90, 74 80, 70 60 C 66 40, 62 15, 50 15 Z" fill="#4D7C0F" />
                      
                      {/* Inner pulp */}
                      <path d="M 50 21 C 41 21, 38 43, 34 61 C 30 78, 42 85, 50 85 C 58 85, 70 78, 66 61 C 62 43, 59 21, 50 21 Z" fill="#A3E635" />
                      
                      {/* Big Seed in center */}
                      <circle cx="50" cy="65" r="14" fill="#854D0E" />
                      <ellipse cx="46" cy="61" r="4" fill="#BBF7D0" opacity="0.15" />

                      {/* Happy Face Details depending on avocadoMood */}
                      <g>
                        {/* Eyes */}
                        {avocadoMood === "ZEN" && (
                          <>
                            <path d="M 38 45 Q 42 41 45 45" stroke="#1E293B" strokeWidth="2.5" strokeLinecap="round" fill="none" />
                            <path d="M 55 45 Q 58 41 61 45" stroke="#1E293B" strokeWidth="2.5" strokeLinecap="round" fill="none" />
                          </>
                        )}
                        {avocadoMood === "SLEEPY" && (
                          <>
                            <line x1="38" y1="46" x2="44" y2="46" stroke="#1E293B" strokeWidth="2.5" strokeLinecap="round" />
                            <line x1="56" y1="46" x2="62" y2="46" stroke="#1E293B" strokeWidth="2.5" strokeLinecap="round" />
                          </>
                        )}
                        {avocadoMood === "HAPPY" && (
                          <>
                            <circle cx="41" cy="45" r="3" fill="#1E293B" />
                            <circle cx="59" cy="45" r="3" fill="#1E293B" />
                            <circle cx="40" cy="44" r="1" fill="#FFFFFF" />
                            <circle cx="58" cy="44" r="1" fill="#FFFFFF" />
                          </>
                        )}
                        {avocadoMood === "CRAZY" && (
                          <>
                            <path d="M 38 42 L 44 48 M 44 42 L 38 48" stroke="#1E293B" strokeWidth="2.5" strokeLinecap="round" />
                            <path d="M 56 42 L 62 48 M 62 42 L 56 48" stroke="#1E293B" strokeWidth="2.5" strokeLinecap="round" />
                          </>
                        )}

                        {/* Blushing cheeks */}
                        <circle cx="37" cy="50" r="4" fill="#f43f5e" opacity={avocadoMood === "SLEEPY" ? 0.3 : 0.75} />
                        <circle cx="63" cy="50" r="4" fill="#f43f5e" opacity={avocadoMood === "SLEEPY" ? 0.3 : 0.75} />

                        {/* Mouth details based on Mood */}
                        {avocadoMood === "ZEN" && (
                          <path d="M 47 49 Q 50 51 53 49" stroke="#1E293B" strokeWidth="2" strokeLinecap="round" fill="none" />
                        )}
                        {avocadoMood === "SLEEPY" && (
                          <circle cx="50" cy="50" r="2" fill="#1E293B" />
                        )}
                        {avocadoMood === "HAPPY" && (
                          <path d="M 46 49 Q 50 54 54 49" stroke="#1E293B" strokeWidth="2" strokeLinecap="round" fill="none" />
                        )}
                        {avocadoMood === "CRAZY" && (
                          <path d="M 45 52 Q 50 46 55 52" stroke="#1E293B" strokeWidth="2.5" strokeLinecap="round" fill="none" />
                        )}
                      </g>

                      {/* Cute Yoga arms sticking up in Namaste! */}
                      <path 
                        d="M 33 48 C 24 45, 26 28, 42 22" 
                        stroke="#A3E635" strokeWidth="5.5" strokeLinecap="round" fill="none"
                      />
                      <path 
                        d="M 67 48 C 76 45, 74 28, 58 22" 
                        stroke="#A3E635" strokeWidth="5.5" strokeLinecap="round" fill="none"
                      />
                      <circle cx="50" cy="21" r="2.5" fill="#FFB703" /> {/* Sparkle at joined hands */}
                    </svg>

                    {/* Floating Sleepy Zs */}
                    {avocadoMood === "SLEEPY" && (
                      <div className="absolute top-4 right-6 text-xl text-indigo-500 font-extrabold animate-bounce select-none">
                        Zzz...
                      </div>
                    )}
                    {/* Floating Sparkles */}
                    {avocadoMood === "CRAZY" && (
                      <div className="absolute top-2 left-6 text-xl text-yellow-500 font-extrabold animate-bounce select-none">
                        💫🤪⚡
                      </div>
                    )}
                  </div>

                  {/* PET MOOD CONTROLLERS GAMEPAD */}
                  <div className="grid grid-cols-2 gap-2 mt-2 w-full">
                    <button
                      onClick={() => {
                        setAvocadoMood("HAPPY");
                        setAvocadoDialogue("Yummm, gluten-free chia-seed avocado muffin! 🧁 My cells are vibrating with happy fuel!");
                        playBubblePop();
                      }}
                      className="p-2 border-2 border-dashed border-pink-300 hover:bg-pink-50 text-[10px] font-black rounded-2xl cursor-pointer text-pink-700 uppercase"
                    >
                      🥑 Feed Muffin
                    </button>
                    <button
                      onClick={() => {
                        setAvocadoMood("CRAZY");
                        setAvocadoDialogue("Hehehe that tickles my green skin! Poking me boosts your blood circulation!");
                        playSillySlide();
                      }}
                      className="p-2 border-2 border-dashed border-amber-300 hover:bg-amber-50 text-[10px] font-black rounded-2xl cursor-pointer text-amber-700 uppercase"
                    >
                      👈 Tickle Me
                    </button>
                    <button
                      onClick={() => {
                        setAvocadoMood("SLEEPY");
                        setAvocadoDialogue("Zzzz... taking a deep fluffy Balasana restful sleep... let the stress fade...");
                        playSnoreSound();
                      }}
                      className="p-2 border-2 border-dashed border-purple-300 hover:bg-purple-50 text-[10px] font-black rounded-2xl cursor-pointer text-purple-700 uppercase"
                    >
                      💤 Sing Lullaby
                    </button>
                    <button
                      onClick={() => {
                        setAvocadoMood("ZEN");
                        setAvocadoDialogue("Breathe in cosmic intelligence... Exhale materialistic attachment. You are peace.");
                        playSuccessFanfare();
                      }}
                      className="p-2 border-2 border-dashed border-emerald-300 hover:bg-emerald-50 text-[10px] font-black rounded-2xl cursor-pointer text-emerald-700 uppercase"
                    >
                      🧘 Meditate Close
                    </button>
                  </div>

                  {/* Synthesizer Synth Soundboard gamepad */}
                  <div className="w-full mt-6 bg-[#FAF5FF] p-3 rounded-2xl border border-purple-100 flex flex-col items-center gap-2">
                    <span className="text-[9px] font-mono uppercase tracking-widest text-slate-700 font-extrabold flex items-center gap-1">
                      🎵 SOUND FX CONTROLS (REAL SYNTH)
                    </span>
                    <div className="grid grid-cols-4 gap-1.5 w-full">
                      <button 
                        onClick={playBubblePop}
                        className="py-1.5 px-1 bg-white hover:bg-purple-100 border border-purple-200 text-[10px] font-extrabold text-slate-700 rounded-lg cursor-pointer"
                        title="Low impedance speaker bubble pop"
                      >
                        Bubble
                      </button>
                      <button 
                        onClick={playSillySlide}
                        className="py-1.5 px-1 bg-white hover:bg-purple-100 border border-purple-200 text-[10px] font-extrabold text-slate-700 rounded-lg cursor-pointer"
                        title="Pitch bending slider frequency oscillator"
                      >
                        Silly
                      </button>
                      <button 
                        onClick={playSnoreSound}
                        className="py-1.5 px-1 bg-white hover:bg-purple-100 border border-purple-200 text-[10px] font-extrabold text-[#5c248d] rounded-lg cursor-pointer"
                        title="Lawnmower hum snore"
                      >
                        Snore
                      </button>
                      <button 
                        onClick={playSuccessFanfare}
                        className="py-1.5 px-1 bg-white hover:bg-purple-100 border border-purple-200 text-[10px] font-extrabold text-pink-600 rounded-lg cursor-pointer"
                        title="Multi-spectral chord victory sound"
                      >
                        Victory!
                      </button>
                    </div>
                  </div>

                  <div className="h-[1px] bg-amber-100 w-full my-6" />

                  {/* Joke Box Speech bubble */}
                  <div className="w-full bg-white rounded-2xl p-5 border border-amber-100 shadow-xs text-center space-y-4 font-serif text-slate-800 relative">
                    <motion.div
                      key={currentJokeIdx}
                      initial={{ opacity: 0, scale: 0.95 }}
                      animate={{ opacity: 1, scale: 1 }}
                      className="space-y-3 relative z-10"
                    >
                      <h4 className="text-sm font-black text-amber-800 uppercase tracking-wider font-mono">
                        {HEALTHY_JOKES[currentJokeIdx].funnyRating}
                      </h4>
                      
                      <p className="text-base font-black font-sans leading-tight text-slate-900">
                        "{HEALTHY_JOKES[currentJokeIdx].setup}"
                      </p>
                      
                      <p className="text-sm text-slate-700 leading-relaxed font-serif italic text-pink-600 font-bold">
                        {HEALTHY_JOKES[currentJokeIdx].punchline}
                      </p>
                    </motion.div>
                  </div>

                  {/* Action launcher */}
                  <button
                    type="button"
                    onClick={() => {
                      // pick random joke that is different from previous
                      let nextJoke = currentJokeIdx;
                      while (nextJoke === currentJokeIdx && HEALTHY_JOKES.length > 1) {
                        nextJoke = Math.floor(Math.random() * HEALTHY_JOKES.length);
                      }
                      setCurrentJokeIdx(nextJoke);
                      playChimeSound();
                    }}
                    className="mt-6 px-6 py-3 bg-[#EAB308] hover:bg-[#CA8A04] text-white font-extrabold rounded-2xl text-xs uppercase tracking-wider shadow-md hover:scale-105 active:scale-95 transition-all flex items-center gap-1.5 cursor-pointer"
                  >
                    ✨ Giggle Me Another!
                  </button>
                </div>

                {/* Extra cute daily habits checklist */}
                <div className="bg-white rounded-3xl border border-purple-100 p-6 shadow-sm space-y-4">
                  <h4 className="text-sm font-mono font-bold uppercase text-[#210833] tracking-widest text-center">
                    🌸 Super Cute Daily Tasks
                  </h4>
                  <ul className="text-xs text-slate-700 font-bold space-y-2.5">
                    <li className="flex items-center gap-2.5 bg-[#FAF5FF] p-2.5 rounded-xl border border-purple-50">
                      <span className="text-base select-none">🥑</span>
                      <span>Eat at least one centered, wholesome green avocado or sweet potato bowl.</span>
                    </li>
                    <li className="flex items-center gap-2.5 bg-[#FAF5FF] p-2.5 rounded-xl border border-purple-50">
                      <span className="text-base select-none">☀️</span>
                      <span>Practice downward-facing dog with cozy blushing checks for 60 seconds!</span>
                    </li>
                    <li className="flex items-center gap-2.5 bg-[#FAF5FF] p-2.5 rounded-xl border border-purple-50">
                      <span className="text-base select-none">😂</span>
                      <span>Share a funny wholesome yoga pun with your nearby Sadhaka yoga friends!</span>
                    </li>
                  </ul>
                </div>

              </div>

            </div>

          </motion.section>
        )}

        {/* JOKES & HASYA YOGA LAUGHTER HIERARCHY PAGE */}
        {activeTab === "jokes" && (
          <motion.section 
            initial={{ opacity: 0, y: 15 }}
            animate={{ opacity: 1, y: 0 }}
            className="space-y-10"
          >
            {/* Header Display */}
            <div className="text-center max-w-2xl mx-auto space-y-3">
              <span className="text-pink-600 font-mono text-xs uppercase tracking-widest font-extrabold bg-pink-100 py-1.5 px-4.5 rounded-full border border-pink-200 inline-block animate-bounce shadow-xs">
                😂 🧘‍♂️ Hasya Yoga Laughs & Puns Hub 🧘‍♂️ 😂
              </span>
              <h2 className="text-3xl font-black text-[#210833] uppercase tracking-tight">The Joyful Sadhaka Giggles</h2>
              <p className="text-xs text-slate-700 font-bold leading-relaxed">
                "Hasya Yoga" (Laughter Meditation) burns away stress hormones, massages internal metabolic organs, and relaxes muscles instantly. Keep your mind joyful and your spine straight!
              </p>
            </div>

            {/* Main Section Grid */}
            <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-start">
              
              {/* LEFT COLUMN (SPAN 8): CATEGORIES, JOKES MATRIX, AND RATING CONTROLE */}
              <div className="lg:col-span-8 space-y-6">
                
                {/* Visual Category Selectors */}
                <div className="bg-white p-4 rounded-2xl border border-purple-100 shadow-xs flex flex-wrap items-center gap-2">
                  <span className="text-[10px] font-mono uppercase text-slate-500 font-extrabold pr-2">
                    📂 Filters:
                  </span>
                  {[
                    { key: "all", label: "🧘‍♀️ All Puns", color: "purple" },
                    { key: "Food & Nutri", label: "🥑 Food & Nutri", color: "pink" },
                    { key: "Animal Yoga", label: "🐕 Animal Yoga", color: "amber" },
                    { key: "Karma & Mind", label: "✨ Karma & Mind", color: "violet" },
                    { key: "Posture Slips", label: "🚶‍♂️ Posture Slips", color: "indigo" }
                  ].map((catItem) => (
                    <button
                      key={catItem.key}
                      onClick={() => {
                        setJokeCategory(catItem.key);
                        playBubblePop();
                      }}
                      className={`text-xs px-3.5 py-1.5 rounded-xl font-extrabold cursor-pointer border-2 transition-all ${
                        jokeCategory === catItem.key
                          ? "bg-[#7C3AED] text-white border-[#7C3AED] shadow-sm transform scale-102"
                          : "bg-purple-50/30 border-purple-100 text-slate-600 hover:border-[#7C3AED]/40 hover:bg-white"
                      }`}
                    >
                      {catItem.label}
                    </button>
                  ))}
                </div>

                {/* Jokes Cards Grid */}
                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  {(() => {
                    const initialList = [
                      ...HEALTHY_JOKES.map((j, idx) => ({
                        id: `initial-${idx}`,
                        setup: j.setup,
                        punchline: j.punchline,
                        funnyRating: j.funnyRating,
                        category: idx === 0 || idx === 3 || idx === 5 ? "Food & Nutri" : idx === 2 ? "Animal Yoga" : idx === 1 ? "Karma & Mind" : "Posture Slips"
                      })),
                      ...userJokes
                    ];
                    
                    const listDisplay = jokeCategory === "all"
                      ? initialList
                      : initialList.filter(j => j.category === jokeCategory);

                    if (listDisplay.length === 0) {
                      return (
                        <div className="md:col-span-2 bg-[#FFFDF6] border-2 border-dashed border-purple-100 rounded-3xl p-12 text-center space-y-4">
                          <span className="text-4xl">🌾</span>
                          <h4 className="text-base font-black text-slate-700 uppercase tracking-tight">No Sacred Puns in this energy center</h4>
                          <p className="text-xs text-slate-500 font-bold max-w-md mx-auto">
                            Add your own hilarious yoga riddle in the form on the right to stoke the laughing fire!
                          </p>
                          <button
                            onClick={() => {
                              setJokeCategory("all");
                              playChimeSound();
                            }}
                            className="bg-purple-100 hover:bg-purple-200 text-[#5c248d] py-1.5 px-4 rounded-xl text-xs font-black uppercase transition-all"
                          >
                            Show All Puns
                          </button>
                        </div>
                      );
                    }

                    return listDisplay.map((jk) => {
                      const votes = jokeVotes[jk.id] || 0;
                      const isVoiceActive = isSpeakingJokeId === jk.id;
                      
                      const handleVote = (jokeId: string) => {
                        const currentVotes = jokeVotes[jokeId] || 0;
                        const updated = { ...jokeVotes, [jokeId]: currentVotes + 1 };
                        setJokeVotes(updated);
                        localStorage.setItem("yogasan_joke_votes_map", JSON.stringify(updated));
                        
                        const pitchRnd = Math.random();
                        if (pitchRnd < 0.4) {
                          playBubblePop();
                        } else if (pitchRnd < 0.7) {
                          playChimeSound();
                        } else {
                          playSillySlide();
                        }
                      };

                      const handleCopy = (jokeId: string, s: string, p: string) => {
                        const str = `Yogasan Master Pulser ✨🧘‍♀️\nSetup: ${s}\nPunchline: ${p}\nKeep laughing!`;
                        if (navigator.clipboard) {
                          navigator.clipboard.writeText(str).then(() => {
                            setCopiedJokeId(jokeId);
                            playChimeSound();
                            setTimeout(() => setCopiedJokeId(null), 2000);
                          });
                        }
                      };

                      const handleVoice = (jokeId: string, s: string, p: string) => {
                        if (isVoiceActive) {
                          if (window.speechSynthesis) {
                            window.speechSynthesis.cancel();
                          }
                          setIsSpeakingJokeId(null);
                          return;
                        }

                        if (!window.speechSynthesis) {
                          return;
                        }

                        window.speechSynthesis.cancel();
                        setIsSpeakingJokeId(jokeId);
                        playSnoreSound(); // fun mechanical start spark!

                        const cleanedS = s.replace(/[^\w\s-!?,.']/gi, '');
                        const cleanedP = p.replace(/[^\w\s-!?,.']/gi, '');

                        const u1 = new SpeechSynthesisUtterance(cleanedS);
                        u1.rate = 0.95;
                        u1.pitch = 1.05;

                        u1.onend = () => {
                          setTimeout(() => {
                            // Check if still same active id (wasn't cancelled)
                            const u2 = new SpeechSynthesisUtterance(cleanedP);
                            u2.rate = 0.92;
                            u2.pitch = 1.2; // cute high punchline finish!
                            u2.onend = () => setIsSpeakingJokeId(null);
                            u2.onerror = () => setIsSpeakingJokeId(null);
                            if (window.speechSynthesis) {
                              window.speechSynthesis.speak(u2);
                            }
                          }, 1000);
                        };

                        u1.onerror = () => setIsSpeakingJokeId(null);
                        window.speechSynthesis.speak(u1);
                      };

                      return (
                        <motion.div
                          key={jk.id}
                          layout
                          className="bg-white border border-purple-100 hover:border-purple-200 p-5 rounded-3xl shadow-xs hover:shadow-md transition-all flex flex-col justify-between space-y-4 select-none relative group"
                        >
                          {/* Inner corner sticker badge */}
                          <div className="absolute top-3 right-3 flex items-center gap-1.5">
                            <span className="text-[8px] font-mono font-black uppercase tracking-wider bg-purple-50 text-purple-700 py-0.5 px-2 rounded-full">
                              {jk.category}
                            </span>
                          </div>

                          <div className="space-y-3">
                            <span className="text-[10px] font-mono uppercase text-pink-600 font-extrabold flex items-center gap-1">
                              🌿 {jk.funnyRating}
                            </span>
                            
                            <h4 className="text-sm font-black text-slate-800 leading-snug">
                              {jk.setup}
                            </h4>
                            
                            <div className="bg-purple-50/40 p-3.5 rounded-xl border border-purple-50/60 transition-all duration-300">
                              <p className="text-xs text-purple-950 font-serif font-black italic">
                                {jk.punchline}
                              </p>
                            </div>
                          </div>

                          {/* Interactive Footer buttons for each joke card */}
                          <div className="flex items-center justify-between pt-3 border-t border-purple-50 text-xs">
                            <div className="flex items-center gap-1.5">
                              <motion.button
                                whileTap={{ scale: 0.88 }}
                                onClick={() => handleVote(jk.id)}
                                className="flex items-center gap-1 text-[10px] font-mono text-pink-700 hover:bg-pink-50 py-1.5 px-2.5 rounded-lg font-bold border border-transparent hover:border-pink-150 cursor-pointer text-[#EAB308]"
                                title="Upvote this with hot spiritual ash!"
                              >
                                <Flame className="w-3.5 h-3.5 fill-current text-pink-500 animate-pulse" />
                                <span>{votes} Likes</span>
                              </motion.button>

                              <button
                                onClick={() => handleCopy(jk.id, jk.setup, jk.punchline)}
                                className={`text-[10px] font-mono py-1.5 px-2 rounded-lg font-bold hover:bg-slate-50 text-slate-600 transition-colors uppercase cursor-pointer ${
                                  copiedJokeId === jk.id ? "text-emerald-600 bg-emerald-50 hover:bg-emerald-50" : ""
                                }`}
                              >
                                {copiedJokeId === jk.id ? "Copied! 📋" : "Share"}
                              </button>
                            </div>

                            {/* Voice robotic narrator button */}
                            <button
                              onClick={() => handleVoice(jk.id, jk.setup, jk.punchline)}
                              className={`flex items-center gap-1 text-[10px] uppercase font-mono py-1.5 px-2.5 rounded-xl border font-black transition-all cursor-pointer ${
                                isVoiceActive
                                  ? "bg-rose-100 hover:bg-rose-200 text-rose-700 border-rose-300 animate-pulse"
                                  : "bg-white hover:bg-purple-50 text-purple-700 border-purple-100"
                              }`}
                              title={isVoiceActive ? "Click to silence current recitation" : "Hear this joke narrated with a hilarious robotic finish!"}
                            >
                              <Volume2 className={`w-3 h-3 ${isVoiceActive ? "animate-spin" : ""}`} />
                              <span>{isVoiceActive ? "Silence 🤫" : "Speak 🗣️"}</span>
                            </button>
                          </div>
                        </motion.div>
                      );
                    });
                  })()}
                </div>

                {/* Hasya Yoga lesson panel */}
                <div className="bg-gradient-to-r from-purple-500 to-indigo-600 p-6 md:p-8 rounded-3xl text-white space-y-4 shadow-sm relative overflow-hidden">
                  <div className="absolute top-0 right-0 p-4 font-mono font-black text-white/10 text-9xl leading-none pointer-events-none select-none">HA</div>
                  <div className="flex items-center gap-2">
                    <span className="bg-white/20 p-2.5 rounded-xl text-yellow-300">
                      <Smile className="w-6 h-6" />
                    </span>
                    <div>
                      <h3 className="text-lg font-black uppercase select-none">What is Laughter (Hasya) Yoga?</h3>
                      <p className="text-[10px] font-mono text-yellow-104 uppercase font-bold">The Science of Metabolic Smiles</p>
                    </div>
                  </div>
                  <p className="text-xs text-white/90 font-medium leading-relaxed font-sans">
                    Developed in 1995, Hasya Yoga proves that voluntary laughter yields the same physiological advantages (increased cell oxygenation, lowered blood pressure, activated abdominal core) as spontaneous laughter. When combined with deep Ayurvedic pranayama breathing, it turns physical laughter into high metabolic core wellness! Give it a go below!
                  </p>
                </div>

              </div>

              {/* RIGHT COLUMN (SPAN 4): CUSTOM CREATOR & LAUGHTER MACHINE */}
              <div className="lg:col-span-4 space-y-6">

                {/* INTERACTIVE LAUGHTER YOGA TRAINING DECK */}
                <div className="bg-gradient-to-b from-[#FFFDF9] to-[#FFF9E6] rounded-3xl border-4 border-dashed border-amber-300 p-6 shadow-sm space-y-4">
                  <div className="flex items-center gap-2">
                    <div className="bg-amber-100 p-2.5 rounded-xl text-amber-700 animate-bounce">
                      <Smile className="w-5 h-5 fill-current" />
                    </div>
                    <div>
                      <h4 className="text-sm font-black text-slate-800 uppercase tracking-tight">Hasya Laughter Trainer</h4>
                      <p className="text-[9px] font-mono uppercase text-[#B45309] font-bold">Interactive Guided Breath Work</p>
                    </div>
                  </div>

                  {hasyaStep === 0 ? (
                    <div className="space-y-4">
                      <p className="text-[11px] text-slate-700 font-semibold leading-relaxed">
                        Ready to exercise your cheeks and core? Start this voluntary laughter circuit. Follow our interactive visual timer and laugh along with us step-by-step!
                      </p>
                      <button
                        onClick={() => {
                          setHasyaStep(1);
                          setHasyaTimer(6); // 6s per step
                          playSuccessFanfare();
                        }}
                        className="w-full bg-[#EAB308] hover:bg-[#CA8A04] text-white py-3 px-4 rounded-2xl text-xs font-black uppercase tracking-wider transition-all shadow-md cursor-pointer border-none flex items-center justify-center gap-1"
                      >
                        ⚡ Start Laughter Circuit
                      </button>
                    </div>
                  ) : (
                    <motion.div 
                      key={hasyaStep}
                      initial={{ scale: 0.95, opacity: 0 }}
                      animate={{ scale: 1, opacity: 1 }}
                      className="bg-white/85 backdrop-blur-xs rounded-2xl p-4.5 border border-amber-200/50 space-y-4"
                    >
                      {/* Step Indicator with Timer */}
                      <div className="flex items-center justify-between gap-2 border-b border-amber-100 pb-3">
                        <span className="text-[10px] font-mono text-amber-800 font-black uppercase">
                          Guided Step {hasyaStep} of 5
                        </span>
                        
                        <div className="flex items-center gap-1.5 bg-amber-100 text-amber-900 font-mono font-black py-0.5 px-2.5 rounded-full text-[11px]">
                          <span>⏰</span>
                          <span>{hasyaTimer}s left</span>
                        </div>
                      </div>

                      {/* Step Contents */}
                      <div className="space-y-2">
                        {hasyaStep === 1 && (
                          <>
                            <h5 className="text-xs font-black text-slate-800 uppercase">Step 1: Rhythm Claps! 👏</h5>
                            <p className="text-xs text-slate-700 font-semibold leading-relaxed">
                              Clap your hands rhythmically in front of your chest and chant aloud: <strong className="text-pink-600">"Ho-ho, Ha-ha-ha!"</strong> to stimulate the thymus gland!
                            </p>
                          </>
                        )}
                        {hasyaStep === 2 && (
                          <>
                            <h5 className="text-xs font-black text-slate-800 uppercase">Step 2: Deep Prana Reach! 🌬️</h5>
                            <p className="text-xs text-slate-700 font-semibold leading-relaxed">
                              Inhale deeply, stretch your hands high to the cosmic heavens! Exhale thoroughly, lowering them and releasing mental blockages.
                            </p>
                          </>
                        )}
                        {hasyaStep === 3 && (
                          <>
                            <h5 className="text-xs font-black text-slate-800 uppercase">Step 3: Silent Avocado Smile! 🥑</h5>
                            <p className="text-xs text-slate-700 font-semibold leading-relaxed">
                              Open your mouth wide, stick your chin forward. Stretch your facial muscles and giggle silently without making any noise! Feel the facial tension release!
                            </p>
                          </>
                        )}
                        {hasyaStep === 4 && (
                          <>
                            <h5 className="text-xs font-black text-slate-800 uppercase">Step 4: Deep Belly Laugh! 😂</h5>
                            <p className="text-xs text-slate-700 font-semibold leading-relaxed">
                              Now let it go completely! Laugh heartily and loudly from deep inside your diaphragm! <strong className="text-[#7C3AED]">"Ha! Ha! Ha! Ha!"</strong>. Feel the endorphins flood your blood!
                            </p>
                          </>
                        )}
                        {hasyaStep === 5 && (
                          <>
                            <h5 className="text-xs font-black text-slate-800 uppercase">Step 5: Cool Heart Harmony 🧘‍♀️</h5>
                            <p className="text-xs text-slate-700 font-semibold leading-relaxed">
                              Bring your palms together at your heart. Close your eyes, inhale smooth cold air, and sit with the vibrant metabolic electrical current in your cells. Namaste.
                            </p>
                          </>
                        )}
                      </div>

                      {/* Step Progress Bar */}
                      <div className="w-full bg-[#FFFDF9]/80 h-2 rounded-full overflow-hidden relative">
                        <div 
                          className="h-full bg-gradient-to-r from-[#EAB308] to-pink-500 transition-all duration-300"
                          style={{ width: `${((7 - hasyaTimer) / 6) * 100}%` }}
                        />
                      </div>

                      {/* Cancel Button */}
                      <button
                        onClick={() => {
                          setHasyaStep(0);
                          setHasyaTimer(0);
                          playSillySlide();
                        }}
                        className="w-full bg-slate-100 hover:bg-slate-200 text-slate-600 text-[10px] font-mono py-1 rounded-xl uppercase transition-all cursor-pointer border-none"
                      >
                        Cancel Practice
                      </button>
                    </motion.div>
                  )}
                </div>

                {/* MAKE YOUR OWN YOGA JOKE (CREATOR DESK) */}
                <div className="bg-white rounded-3xl border border-purple-100 p-6 shadow-sm space-y-4">
                  <div className="flex items-center gap-2 border-b border-purple-50 pb-3">
                    <span className="text-lg">🎨</span>
                    <div>
                      <h4 className="text-sm font-black text-[#210833] uppercase tracking-tight">Sadhaka Joke Creator</h4>
                      <p className="text-[9px] font-mono uppercase text-slate-500 font-bold">Launch Custom Karma Ridles</p>
                    </div>
                  </div>

                  <form 
                    onSubmit={(e) => {
                      e.preventDefault();
                      if (!newSetup.trim() || !newPunchline.trim()) {
                        playSillySlide();
                        return;
                      }

                      const newObj = {
                        id: `user-${Date.now()}`,
                        setup: newSetup,
                        punchline: newPunchline,
                        funnyRating: `${newEmoji} Custom Squeeze Index: 10/10`,
                        category: newCategory
                      };

                      const updatedList = [newObj, ...userJokes];
                      setUserJokes(updatedList);
                      localStorage.setItem("yogasan_user_jokes", JSON.stringify(updatedList));

                      setNewSetup("");
                      setNewPunchline("");
                      playSuccessFanfare();
                    }}
                    className="space-y-4 text-xs font-semibold"
                  >
                    {/* Setup Input */}
                    <div className="space-y-1.5">
                      <label className="text-[10px] font-mono uppercase text-slate-600 font-bold block">
                        Joke Question or Setup:
                      </label>
                      <input
                        type="text"
                        value={newSetup}
                        onChange={(e) => setNewSetup(e.target.value)}
                        placeholder="e.g. Why was the mattress so bad at yoga?"
                        className="w-full p-3.5 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:outline-none text-slate-900 text-xs font-bold font-sans"
                        maxLength={120}
                        required
                      />
                    </div>

                    {/* Punchline Input */}
                    <div className="space-y-1.5">
                      <label className="text-[10px] font-mono uppercase text-slate-600 font-bold block">
                        The Punchline Answer:
                      </label>
                      <textarea
                        value={newPunchline}
                        onChange={(e) => setNewPunchline(e.target.value)}
                        placeholder="e.g. Because it always folded under mild pressure!"
                        className="w-full p-3.5 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:outline-none text-slate-900 text-xs font-bold font-sans h-16 resize-none"
                        maxLength={180}
                        required
                      />
                    </div>

                    {/* Category Selector */}
                    <div className="grid grid-cols-2 gap-2 text-[10px] font-mono">
                      <div className="space-y-1">
                        <label className="uppercase text-slate-500 font-bold">Category:</label>
                        <select
                          value={newCategory}
                          onChange={(e) => setNewCategory(e.target.value)}
                          className="w-full p-2 bg-slate-50 border border-slate-200 rounded-lg text-slate-800 font-bold font-sans focus:outline-none"
                        >
                          <option value="Food & Nutri">🥑 Food Puns</option>
                          <option value="Animal Yoga">🐕 Animal Yoga</option>
                          <option value="Karma & Mind">✨ Karma & Mind</option>
                          <option value="Posture Slips">🏃‍♂️ Posture Slips</option>
                        </select>
                      </div>

                      <div className="space-y-1">
                        <label className="uppercase text-slate-500 font-bold">Rating Logo:</label>
                        <select
                          value={newEmoji}
                          onChange={(e) => setNewEmoji(e.target.value)}
                          className="w-full p-2 bg-slate-50 border border-slate-200 rounded-lg text-slate-800 font-bold font-sans focus:outline-none"
                        >
                          <option value="🥑">🥑 Avocado</option>
                          <option value="🔥">🔥 Tapas Flame</option>
                          <option value="💖">💖 Love Pulse</option>
                          <option value="✨">✨ Karma Gem</option>
                          <option value="😂">😂 Laughing Face</option>
                        </select>
                      </div>
                    </div>

                    <button
                      type="submit"
                      className="w-full bg-[#7C3AED] hover:bg-[#6D28D9] text-white py-3 rounded-2xl text-[11px] font-black uppercase tracking-wider transition-colors shadow-xs cursor-pointer border-none flex items-center justify-center gap-1.5"
                    >
                      ✨ Launch Cosmic Joke!
                    </button>
                  </form>
                </div>

              </div>

            </div>
          </motion.section>
        )}

      </main>

      {/* GLOBAL CHIP/MODAL DRAWER FOR SELECTED POSE DETAIL POPUP */}
      <AnimatePresence>
        {selectedPose && (
          <motion.div 
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="fixed inset-0 z-50 bg-[#210833]/60 backdrop-blur-sm flex items-center justify-center p-4"
            id="pose-detail-modal"
          >
            <motion.div
              initial={{ scale: 0.95, y: 15 }}
              animate={{ scale: 1, y: 0 }}
              exit={{ scale: 0.95, y: 15 }}
              className="bg-white border border-purple-100 w-full max-w-2xl rounded-2xl overflow-hidden shadow-2xl relative"
            >
              <button
                onClick={() => { setSelectedPose(null); setVoiceExplanation(null); }}
                className="absolute top-4 right-4 p-1.5 rounded-full bg-purple-50 border border-purple-150 hover:bg-purple-100 text-[#5c248d] transition-all cursor-pointer z-10"
              >
                <X className="w-5 h-5" />
              </button>

              <div className="grid grid-cols-1 md:grid-cols-12">
                
                {/* SVG Visualizer left */}
                <div className="md:col-span-5 bg-purple-50/50 p-6 flex flex-col items-center justify-center border-b md:border-b-0 md:border-r border-purple-100">
                  <PoseVisualizer poseId={selectedPose.id} isBreathing={true} size={180} />
                  
                  <span className="text-[10px] tracking-wide text-[#5c248d] font-serif font-bold italic mt-3 block text-center">
                    {selectedPose.sanskritName} — Alignment Cycle
                  </span>

                  {/* Pose vocal explanation trigger */}
                  <button
                    onClick={() => triggerVoiceExplanation(selectedPose)}
                    className="mt-6 w-full py-2 bg-white border border-purple-200 text-xs font-bold rounded-xl text-purple-700 hover:bg-purple-50 transition-all flex items-center justify-center gap-1 cursor-pointer shadow-xs"
                  >
                    <Volume2 className="w-4 h-4 text-pink-600" />
                    {voiceExplanation ? "Stop Voice Guide" : "Play Voice Guide"}
                  </button>

                  {voiceExplanation && (
                    <p className="mt-3 text-[10px] text-pink-600 font-bold italic font-serif leading-relaxed text-center px-2">
                      {voiceExplanation}
                    </p>
                  )}
                </div>

                {/* Text details right */}
                <div className="md:col-span-7 p-6 space-y-4">
                  <div>
                    <span className="text-[9px] font-mono text-pink-600 bg-pink-50 border border-pink-100 px-2 py-0.5 rounded uppercase leading-none font-bold">
                      Interactive Pose Details
                    </span>
                    <h3 className="text-2xl font-extrabold text-[#210833] mt-1">
                      {selectedPose.name}
                    </h3>
                    <p className="text-xs text-slate-600 font-semibold">
                      Standard duration timing: <strong>{selectedPose.duration}</strong> | Estimated Calorie Burn: <strong>{selectedPose.calories} kcal</strong>
                    </p>
                  </div>

                  <div className="h-[1px] bg-purple-100" />

                  {/* Alignment Steps */}
                  <div className="space-y-1.5">
                    <h4 className="text-xs font-mono font-bold uppercase text-[#210833] tracking-wider">Instructions</h4>
                    <ol className="list-decimal list-inside text-xs text-slate-750 space-y-1 font-semibold leading-relaxed">
                      {selectedPose.steps.map((st, si) => (
                        <li key={si} className="text-slate-700">{st}</li>
                      ))}
                    </ol>
                  </div>

                  {/* Benefits */}
                  <div className="space-y-1">
                    <h4 className="text-xs font-mono font-bold uppercase text-emerald-700 tracking-wider">Key Benefits</h4>
                    <ul className="list-disc list-inside text-xs text-slate-650 space-y-0.5 font-bold leading-relaxed">
                      {selectedPose.benefits.map((be, bi) => (
                        <li key={bi}>{be}</li>
                      ))}
                    </ul>
                  </div>

                  {/* Precautions */}
                  <div className="space-y-1">
                    <h4 className="text-xs font-mono font-bold uppercase text-amber-700 tracking-wider">Injury Guard Precautions</h4>
                    <ul className="list-disc list-inside text-xs text-slate-650 space-y-0.5 font-bold leading-relaxed">
                      {selectedPose.precautions.map((pr, pi) => (
                        <li key={pi}>{pr}</li>
                      ))}
                    </ul>
                  </div>

                </div>

              </div>

            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>

      {/* FOOTER */}
      <footer className="mt-16 bg-white border-t border-purple-100 px-4 py-8 lg:px-8 z-10" id="footer-section">
        <div className="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-12 gap-8 items-start">
          
          {/* Logo & Credits Column */}
          <div className="md:col-span-5 space-y-4">
            <div className="flex items-center gap-3">
              <Logo size={48} animate={false} />
              <div>
                <strong className="text-base font-extrabold bg-gradient-to-r from-purple-900 to-[#5c248d] bg-clip-text text-transparent uppercase tracking-wider">Yogasan Master</strong>
                <p className="text-[10px] font-mono text-purple-700 uppercase font-bold">The AI Physical & Spiritual Companion</p>
              </div>
            </div>
            
            <p className="text-xs text-slate-600 font-semibold leading-relaxed max-w-sm">
              Crafting premium futuristic health ecosystems. Bridging centuries of Indian scriptures and Patanjali wisdom with modern, safe digital diagnostic systems.
            </p>

            <div className="text-xs font-bold text-pink-600 font-serif">
              Created for the world by <span className="text-[#5c248d] hover:text-pink-600 transition-colors font-bold">TASHI & Shubhi</span>
            </div>
          </div>

          {/* Nav links Columns */}
          <div className="md:col-span-3 space-y-3 text-xs">
            <h4 className="text-xs font-mono font-bold text-[#210833] uppercase tracking-widest">Platform Map</h4>
            <ul className="space-y-2 text-slate-600 font-semibold">
              <li><button onClick={() => setActiveTab("home")} className="hover:text-pink-600 pointer-events-auto">Curated Home Sanctuary</button></li>
              <li><button onClick={() => setActiveTab("assessment")} className="hover:text-pink-600 pointer-events-auto">AI Intake Assessment</button></li>
              <li><button onClick={() => setActiveTab("poses")} className="hover:text-pink-600 pointer-events-auto">Interactive Poses Matrix</button></li>
              <li><button onClick={() => setActiveTab("tracker")} className="hover:text-pink-600 pointer-events-auto">Personal Wellness Tracker</button></li>
              <li><button onClick={() => setActiveTab("cute")} className="hover:text-pink-600 pointer-events-auto">🥗 Cooking & Pet</button></li>
              <li><button onClick={() => setActiveTab("jokes")} className="hover:text-pink-600 pointer-events-auto">😂 Jokes Page</button></li>
            </ul>
          </div>

          {/* Newsletter Column */}
          <div className="md:col-span-4 space-y-4">
            <h4 className="text-xs font-mono font-bold text-pink-600 uppercase tracking-widest font-bold">Newsletter Intention</h4>
            <p className="text-xs text-slate-600 font-semibold leading-relaxed">
              Subscribe to download safe weekly biomechanics, nutrition strategies, and daily meditative guidelines.
            </p>
            <div className="flex gap-2">
              <input
                type="email"
                placeholder="yogi@sanctuary.com"
                className="bg-purple-50/50 border border-purple-200 rounded-xl px-4 py-2.5 text-xs text-slate-800 placeholder-slate-400 focus:outline-none focus:border-purple-400 flex-grow"
              />
              <button
                onClick={() => alert("Namaste 🙏. Newsletter signup registered inside client database.")}
                className="px-4 py-2 bg-[#7c3aed] text-white hover:bg-[#6d28d9] rounded-xl font-bold text-xs uppercase cursor-pointer shrink-0"
              >
                Incline
              </button>
            </div>
          </div>

        </div>

        <div className="h-[1px] bg-purple-100 my-6" />

        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row items-center justify-between text-[10px] text-slate-500 font-mono gap-4 uppercase leading-none font-bold">
          <span>&copy; 2026 Yogasan Master. Absolute spiritual & physical sovereignty reserved.</span>
          <div className="flex items-center gap-4">
            <a href="#" className="hover:text-slate-800">Privacy Policy</a>
            <span>&bull;</span>
            <a href="#" className="hover:text-slate-800">Terms of Sanctuary</a>
            <span>&bull;</span>
            <span className="text-pink-600">Created by TASHI & Shubhi</span>
          </div>
        </div>
      </footer>

    </div>
  );
}
